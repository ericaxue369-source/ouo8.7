# 白盒模块 PRD 简版

版本：V1.1  
更新时间：2026-08-11  
用途：给产品、前端、后端、算法、测试统一理解白盒本期开发范围  
对应原型：`鸥我白盒8.2-改ui-开发需求备注版-GitHub最终交付版.html`

## 1. 一句话说明

白盒是首页的双列瀑布流笔记模块。用户可以阅读、点赞、收藏、评论、分享，也可以看到平台在用户授权后自动生成并发布的 AI 笔记。

本期白盒重点做三件事：

1. 首页白盒内容流和笔记互动。
2. 用户授权后的 AI 自动发布。
3. 白盒打开后的底图配置和轻量盲盒体验。

## 2. 本期开发范围

### 2.1 需要做

1. 首页白盒双列瀑布流。
2. 首页搜索框和分类 Tab。
3. 白盒笔记详情页。
4. 点赞、收藏、评论、回复、分享、关注、开聊。
5. 评论置顶、取消置顶、删除。
6. 登录后自动发布授权弹窗。
7. 用户授权后的自动发文任务。
8. 白盒打开后的底图展示和后台配置。
9. 自动发布笔记同步到「我的-外表-笔记」。

### 2.2 本期不做

1. 咨询服务。
2. 擅长领域。
3. 支付、下单、价格、交易闭环。
4. 数字图书馆内容参与白盒自动发布。
5. 复杂推荐算法完整建设。

## 3. 内容来源

白盒内容有两类：

| 类型 | 说明 | 是否展示在首页 |
|---|---|---|
| 用户手动发布笔记 | 用户自己发布的公开笔记 | 是 |
| AI 自动发布笔记 | 用户授权后，平台根据名片标签或后台知识库生成 | 是 |

数字图书馆的周报不参与白盒自动发布。白盒自动发布只使用：

1. 用户名片标签。
2. 用户后台知识库内容。

## 4. 登录与授权流程

### 4.1 老用户

老用户升级后首次进入首页：

1. 展示「白盒自动发布授权」弹窗。
2. 用户点击「允许自动发布」后，开启自动发布。
3. 用户点击「暂不允许」后，本次不自动发布。
4. 用户后续可在设置中重新开启或关闭。

### 4.2 新用户

新用户登录后：

1. 进入「和鸥我 CEO Tracy 聊」页面。
2. 用户可以聊天生成资料，也可以跳过。
3. 进入首页。
4. 首页展示「白盒自动发布授权」弹窗。

### 4.3 授权弹窗文案

标题：白盒自动发布授权

正文：

> 系统会根据你的名片标签或知识库自动生成笔记，并发布到平台展示。是否允许鸥我在你授权后自动发布？

按钮：

1. 允许自动发布
2. 暂不允许

规则：

1. 同一授权协议版本只弹一次。
2. 用户拒绝后，不生成、不发布自动笔记。
3. 用户关闭授权后，未执行任务取消。
4. 已发布的历史笔记保留。

## 5. 自动发布规则

### 5.1 进入自动发布池的条件

用户必须同时满足：

1. 用户账号状态正常。
2. 已允许白盒自动发布。
3. 至少填写过 1 个名片标签，或后台知识库至少有 1 条内容。
4. 没有被平台风控限制发文。

如果用户只授权，但没有名片标签、也没有知识库内容，则不自动发布。等用户补充标签或知识库后，再进入自动发布池。

### 5.2 每周发布数量

1. 每个用户每自然周最多自动发布 3 篇。
2. 自然周按北京时间计算。
3. 每周一 00:00 重置配额。
4. 每周一预生成本周自动发布任务。

### 5.3 可发布时间段

| 时间段 | 时间 |
|---|---|
| S1 | 06:00-08:00 |
| S2 | 08:00-10:00 |
| S3 | 12:00-14:00 |
| S4 | 17:00-23:00 |

非以上时间段禁止自动发布。

### 5.4 两条硬规则

同一用户一周最多 3 篇自动发布笔记，必须满足：

1. 3 篇必须分配到 3 个不同自然日。
2. 3 篇必须使用 3 个不同时间段。

示例：

| 是否可用 | 安排 |
|---|---|
| 可用 | 周二 S1、周四 S3、周日 S4 |
| 不可用 | 周二 S1、周二 S3、周日 S4 |
| 不可用 | 周二 S1、周四 S1、周日 S4 |

### 5.5 失败处理

1. 发送失败时，只在原时间窗口内重试。
2. 原时间窗口错过后，本次推送顺延到下一周。
3. 顺延任务仍需满足不同日期、不同时间段规则。
4. 用户关闭授权后，未执行任务取消。

## 6. AI 内容生成规则

每次自动生成笔记时，只取一个核心素材：

1. 1 个名片标签，或
2. 1 条后台知识库内容。

AI 围绕这个核心素材扩展生成笔记。

生成要求：

1. 笔记标题、正文、分类、标签必须完整。
2. 可以生成同一类型的笔记，但内容不能重复。
3. 不能连续发布高度相似的标题或正文。
4. 自动生成笔记必须展示 AI 标识。
5. 发布前必须过内容安全审核。
6. 用户可以删除自动发布笔记。
7. 用户可以把自动发布笔记设为仅自己可见。

## 7. 首页白盒展示

### 7.1 瀑布流

1. 首页白盒使用双列瀑布流。
2. 卡片高度由内容自动撑开。
3. 标题最多展示 3 行，超出省略。
4. 首页卡片不展示摘要。
5. 卡片最小高度 168px。
6. 分页加载后不能重叠、不能出现大空洞。

### 7.2 搜索

搜索框文案：

> 搜索经验...

搜索规则：

1. 搜索范围：标题、正文、作者、分类、标签。
2. 搜索只影响内容列表，不改变顶部 Tab。
3. 在某个 Tab 内搜索时，只搜索当前 Tab 内容。
4. 无结果时展示空态。

### 7.3 分类 Tab

原型展示：

> 全部、大学、求职、创业、科研、考研、实习、科普、生活、搞笑、奇幻、职场

正式规则：

1. `全部` 永远展示在第一个。
2. 其他 Tab 根据已发布、公开、审核通过内容的标签或分类自动生成。
3. 没有内容的 Tab 不展示。
4. 后端负责同义词归一，避免重复 Tab。
5. 除 `全部` 外最多展示 12 个 Tab。
6. 客户端只渲染接口返回，不在本地写死分类。

## 8. 白盒打开和底图规则

### 8.1 打开流程

用户点击白盒后：

1. 先展示本次命中的展开底图或色块。
2. 展示 500-800ms。
3. 自动进入笔记详情页。

如果底图加载失败，使用兜底渐变，不影响进入详情页。

### 8.2 底图命中

底图只分两类：

| 类型 | 概率 | 说明 |
|---|---:|---|
| 普通底图 | 90% | 大多数情况下展示 |
| 稀有底图 | 10% | 少量出现，用于制造惊喜 |

规则：

1. 命中由服务端计算。
2. 客户端不参与随机。
3. 稀有底图内部支持运营配置每张图或色块的权重。
4. 运营可以在后台调整稀有底图资源和权重。
5. 同一用户第一次打开同一白盒后，底图结果绑定。
6. 同一用户再次打开同一白盒，展示同一底图。

### 8.3 盲盒体验规则

盲盒体验只保留体验、防刷和稳定性规则：

1. 普通/稀有整体比例固定为 90% / 10%。
2. 稀有底图库内部按运营配置权重抽取。
3. 连续打开或阅读的 6 个白盒，主色不得完全重复。
4. 重复打开、快速退出、频繁刷新，不提高稀有概率。
5. 点赞、收藏、评论、分享、阅读时长不提高稀有概率。
6. 用户前台不展示“再开几次必出”等规律提示。
7. 稀有资源下线后，服务端回退到其他稀有资源；没有可用稀有资源时回退普通底图。

### 8.4 暗黑模式

每个底图配置应支持：

1. 浅色模式资源。
2. 深色模式资源。
3. 文字色。
4. 遮罩透明度。
5. 图片加载失败兜底色。

如果没有深色图，必须使用深色兜底渐变，不允许直接强制反色。

## 9. 笔记详情和互动

笔记详情页需要支持：

1. 阅读正文。
2. 点赞/取消点赞。
3. 收藏/取消收藏。
4. 评论。
5. 回复评论。
6. 分享。
7. 关注作者。
8. 点击作者头像进入名片详情。
9. 开聊。

互动计数规则：

1. 点赞成功后数量 +1，取消后 -1。
2. 收藏成功后数量 +1，取消后 -1。
3. 评论成功后评论数 +1。
4. 分享只记录分享行为，不改变点赞、收藏、评论数。
5. 关注成功后对方粉丝数 +1。
6. 开聊成功后互动数按后台统计口径增加。

## 10. 评论规则

### 10.1 评论排序

首页笔记评论默认使用综合排序。

排序优先级：

1. 作者置顶评论。
2. 点赞多、回复多的评论。
3. 作者回复过的评论。
4. 带图或内容完整的评论。
5. 权重接近时，新评论靠前。

笔记预览区露出的少量评论，也从综合排序结果中取。

### 10.2 置顶规则

1. 只有笔记作者可以置顶评论。
2. 同一篇笔记只能置顶 1 条评论。
3. 如果已经有置顶评论，再置顶另一条时，需要弹窗确认替换。
4. 操作入口是点击评论条目弹出操作面板。
5. 操作面板按权限展示：置顶评论、取消置顶、删除评论。

### 10.3 删除规则

1. 笔记作者可以删除自己笔记下的评论。
2. 评论作者可以删除自己的评论。
3. 删除一级评论时，其子评论也删除或按后端统一策略隐藏。
4. 被平台审核隐藏的评论，普通用户不可见。

## 11. 我的页面同步

自动发布成功后，需要同步到：

1. 我的 → 外表 → 笔记。
2. 我的 → 外表 → 获赞与收藏统计。
3. 名片详情中的获赞、收藏、互动数据。

我的页面排序：

1. 我的笔记按发布时间倒序。
2. 我的评论按评论发布时间倒序。
3. 我的收藏按收藏时间倒序。
4. 最新的在最前。

## 12. 后台需要支持

### 12.1 白盒视觉配置

运营后台需要支持：

1. 上传普通底图。
2. 上传稀有底图。
3. 配置稀有底图单图权重。
4. 配置浅色/深色资源。
5. 配置主色。
6. 配置遮罩和文字色。
7. 发布、下线、回滚。
8. 预览白盒卡片和详情页效果。

### 12.2 知识库分类

后台需要支持知识库分类，作为白盒 AI 生成笔记的素材管理能力。

客户端本期只展示生成后的白盒内容，不在本地维护知识库分类规则。

## 13. 关键接口建议

| 接口 | 用途 |
|---|---|
| 查询首页白盒列表 | 返回瀑布流内容、Tab、互动状态 |
| 查询笔记详情 | 返回正文、作者、评论、底图配置 |
| 更新自动发布授权 | 开启或关闭自动发布 |
| 查询自动发布状态 | 设置页展示开关状态 |
| 点赞/取消点赞 | 更新点赞状态和计数 |
| 收藏/取消收藏 | 更新收藏状态和计数 |
| 评论/回复 | 发布评论和回复 |
| 删除评论 | 删除评论或回复 |
| 置顶/取消置顶评论 | 作者管理评论 |
| 分享上报 | 记录分享行为 |
| 关注/取消关注 | 更新关注关系 |
| 开聊上报 | 记录互动次数 |

## 14. 验收清单

开发完成后至少验证：

1. 老用户首次进入首页会看到授权弹窗。
2. 新用户 Tracy 聊天后进入首页会看到授权弹窗。
3. 拒绝授权后不生成自动发布任务。
4. 允许授权但没有名片标签和知识库时，不生成自动发布任务。
5. 用户补充标签或知识库后，可以进入自动发布池。
6. 每个用户每周最多自动发布 3 篇。
7. 3 篇自动笔记日期不重复、时间段不重复。
8. 非 S1/S2/S3/S4 时间段不会发布。
9. 发送失败只在原窗口重试，错过窗口后顺延下周。
10. AI 每次只取 1 个标签或 1 条知识库内容生成。
11. 同一用户自动发布内容不重复。
12. 首页白盒是双列瀑布流，长标题最多 3 行。
13. 搜索无结果有空态。
14. Tab 由后端返回，客户端不写死。
15. 点击白盒后展示底图 500-800ms 再进详情。
16. 普通/稀有底图整体命中比例为 90% / 10%。
17. 稀有底图内部按后台权重命中。
18. 重复打开同一白盒展示同一底图。
19. 重复刷新、快速退出不提高稀有概率。
20. 点赞、收藏、评论、分享、关注、开聊均可用。
21. 作者可以置顶、取消置顶、删除评论。
22. 评论作者可以删除自己的评论。
23. 自动发布笔记同步到我的笔记列表。
24. 暗黑模式下底图、文字、按钮都清晰可读。

## 15. 开发口径总结

1. 白盒是首页内容流。
2. 自动发布必须先授权。
3. 授权后还要满足标签或知识库门槛。
4. 每周最多 3 篇。
5. 3 篇必须不同日期、不同时间段。
6. 每次 AI 只取 1 个素材生成。
7. 首页是双列瀑布流。
8. 底图概率固定 90% 普通、10% 稀有。
9. 稀有图内部权重由运营配。
10. 客户端只展示结果，不自己算概率。
