# 首页白盒自动发布 PRD V1.0

更新时间：2026-08-11  
对应原型：`/Users/erica/Documents/Codex/2026-08-02/10-ui-apple-human-interface-guidelines/outputs/鸥我白盒8.2-改ui-开发需求备注版-GitHub最终交付版.html`  
面向对象：产品、iOS、Android、鸿蒙、后端、算法、测试

## 1. 背景与目标

本版本在首页新增「白盒自动发布」授权能力。用户登录后进入首页时，平台需要请求用户授权：是否允许平台依据用户填写的名片标签或后台知识库自动生成笔记内容，并按平台规则定时发布到首页白盒内容流。

目标：

1. 老用户升级后首次进入首页，展示自动发布授权确认弹窗。
2. 新用户登录后先进入与「鸥我 CEO Tracy」聊天页，聊完或跳过后进入首页，并展示自动发布授权确认弹窗。
3. 用户允许授权后，平台按「每人每周最多 3 篇、不同自然日、不同时间段」规则生成并发布白盒笔记。
4. 用户拒绝授权后，不生成、不发布自动白盒笔记；后续可在设置页「AI 智库自动发文」手动开启。
5. 首页白盒内容流展示自动发布笔记，并支持搜索、分类筛选、阅读、点赞、评论、收藏、分享、关注作者、开聊等交互。

## 2. 需求范围

包含：

1. 首页首次授权弹窗。
2. 设置页「AI 智库自动发文」开关。
3. 首页白盒笔记列表与笔记详情页展示。
4. 自动发文授权状态管理。
5. 每周定时任务预生成与调度。
6. 自动生成笔记发布后的首页展示、我的主页笔记同步。
7. 发布失败、窗口错过、用户关闭授权等异常处理。

不包含：

1. 具体 AI 内容生成模型策略。
2. 人工审核后台完整设计。
3. 知识库训练流程改版。
4. 个性化推荐算法完整策略。

## 3. 用户流程

### 3.1 老用户升级后流程

1. 用户完成登录。
2. 系统判断用户为老用户，且当前授权协议版本未确认。
3. 用户进入首页。
4. 首页弹出「白盒自动发布授权」确认弹窗。
5. 用户点击「允许自动发布」：
   - 授权状态更新为 `enabled`。
   - 若当前自然周仍有可用日期和时间段，立即补生成本周任务。
   - 首页正常展示。
6. 用户点击「暂不允许」、关闭按钮或点击蒙层：
   - 授权状态更新为 `denied`。
   - 不生成自动发布任务。
   - 首页正常展示。

### 3.2 新用户登录后流程

1. 用户进入登录页。
2. 用户可选择：
   - 手机号码一键登录。
   - 微信登录。
   - 其他登录：手机号验证码登录、手机号密码登录、忘记密码。
3. 登录成功后进入「和鸥我 CEO Tracy 聊」页面。
4. 聊天页用于生成用户初始资料与知识库线索。
5. 用户可以：
   - 完成聊天后进入首页。
   - 点击跳过，不聊直接进入首页。
6. 用户首次进入首页时展示「白盒自动发布授权」确认弹窗。

### 3.3 后续开关流程

入口：我的 → 设置 → AI 与通知 → AI 智库自动发文。

1. 开关开启：
   - 若用户已同意授权协议，直接将状态改为 `enabled`。
   - 若用户未同意或协议版本更新，需再次弹出授权说明后开启。
2. 开关关闭：
   - 授权状态改为 `disabled_by_user`。
   - 未执行的自动发布任务全部取消。
   - 已发布内容不删除。
3. 用户再次开启：
   - 恢复为 `enabled`。
   - 按当前自然周剩余日期和时间段补生成任务。

## 4. 页面与字段需求

### 4.1 首页授权弹窗

弹窗类型：底部确认弹窗。  
触发页面：首页。  
触发条件：用户未确认当前自动发布授权协议版本，且进入首页。

字段：

| 字段 | 文案/规则 | 是否必需 |
|---|---|---|
| 标题 | 白盒自动发布授权 | 是 |
| 正文 | 系统会根据你的名片标签或知识库自动生成笔记，并发布到平台展示。是否允许鸥我在你授权后自动发布？ | 是 |
| 拒绝按钮 | 暂不允许 | 是 |
| 同意按钮 | 允许自动发布 | 是 |
| 关闭按钮 | 右上角关闭 | 是 |
| 蒙层 | 点击后等同暂不允许 | 是 |

建议补充文案：

> 允许后，平台每周最多自动发布 3 篇笔记。发布时间会根据平台规则分配，你可随时在设置中关闭。

交互：

1. 点击「允许自动发布」后关闭弹窗，保存授权，展示首页。
2. 点击「暂不允许」、关闭按钮、蒙层后关闭弹窗，保存拒绝状态，展示首页。
3. 同一授权协议版本只弹一次；协议版本更新后允许再次弹出。
4. 弹窗层级高于首页、底部导航和 Toast。

### 4.2 设置页开关

入口：我的 → 设置 → AI 与通知。

原型字段：

| 字段 | 当前原型文案 | 说明 |
|---|---|---|
| 分组标题 | AI 与通知 | 设置页分组 |
| 设置项 | AI 智库自动发文 | 自动发布功能入口 |
| 描述 | AI 依据名片标签或知识库自动创作、定时发布内容 | 功能说明 |
| 控件 | Toggle 开关 | 开启/关闭授权执行 |

状态：

| 状态 | 开关 | 后端授权状态 |
|---|---|---|
| 未确认 | 关闭 | `unconfirmed` |
| 已允许 | 开启 | `enabled` |
| 已拒绝 | 关闭 | `denied` |
| 用户关闭 | 关闭 | `disabled_by_user` |
| 平台禁用 | 关闭不可点 | `disabled_by_platform` |

### 4.3 首页白盒列表

原型结构：

1. 顶部搜索框。
2. 分类 Tab。
3. 双列瀑布流白盒卡片。
4. 底部导航。

瀑布流布局规则：

1. 首页白盒列表必须采用双列瀑布流（masonry）布局，不使用等高卡片。
2. 两列独立流式排列，下一张卡片紧跟当前列上一张卡片底部，不强制与另一列同一行对齐。
3. 卡片高度由内容自然撑开：标题、作者信息、互动信息越多，卡片高度自动增加。
4. 卡片宽度由容器和列数决定；移动端默认 2 列，左右页面边距 16px，列间距 14px。
5. 卡片最小高度为 168px；内容不足时保持最小高度，内容超出时自动增高。
6. 标题最多展示 3 行，超出省略；摘要不再展示。
7. 禁止用固定高度裁切整张卡片；只能对标题等文本字段做行数截断。
8. 刷新、分页加载、搜索和分类切换后需重新计算瀑布流高度，避免卡片重叠或留大块空白。
9. 图片、头像、AI 标识加载失败时不得影响卡片高度计算；需展示占位资源。
10. Android、iOS、鸿蒙三端需保持双列瀑布流的认知一致性；实现方式可分别使用平台原生列表能力。

搜索框：

| 字段 | 规则 |
|---|---|
| placeholder | 搜索经验… |
| 搜索范围 | 标题、正文、作者名、分类、标签 |可参考之前名片检索的交互逻辑
| 清空按钮 | 输入非空时显示 |
| 空态文案 | 没有找到「关键词」 |

分类 Tab：

首页分类 Tab 不在客户端写死，需根据已发布内容的标签/分类自动生成。

基础规则：

1. `全部` 永远固定展示在第一个。
2. 其他 Tab 来源于已发布、公开、审核通过的白盒内容标签/分类。
3. 只有标签下存在至少 1 篇可展示内容时，才展示该 Tab。
4. 自动发布内容和手动发布内容都可参与 Tab 统计，但内容必须满足 `published + public + approved`。
5. 客户端只渲染接口返回的 Tab，不允许写死 `大学`、`职场`、`实习` 等固定分类。
6. 当前原型中的 `全部`、`大学`、`求职`、`创业`、`科研`、`考研`、`实习`、`科普`、`生活`、`搞笑`、`奇幻` `职场`虽仅作为示例展示，前期也代表tab顺序。

Tab 生成规则：

| 规则 | 说明 |
|---|---|
| 数据来源 | 已发布、公开、审核通过的白盒内容 |
| 生成依据 | 内容的 `category`、`tag` 或运营配置的标签映射 |
| 固定项 | `全部` 固定第 1 位 |
| 展示上限 | 除 `全部` 外最可以多展示 Tab |
| 最小内容数 | 默认 1 篇即可展示，可由后台配置调整 |
| 刷新频率 | 默认 10 分钟缓存；内容发布/下架后服务端可主动刷新 |
| 空 Tab | 不展示 |
| 当前 Tab 失效 | 自动回到 `全部` |

Tab 排序规则：

1. `全部` 永远第 1 位。
2. 运营置顶 Tab 优先。
3. 非置顶 Tab 按近 7 天发布量降序。
4. 发布量相同时，按最近一篇内容发布时间倒序。
5. 仍相同时，按后台配置的 `sort_order` 升序。

标签归一化：

1. 后端需维护标签映射，避免同义标签生成多个 Tab。
2. 示例：`科学科普`、`科普` 可归一为 `科普`。
3. 示例：`留学申请`、`留学文书` 可归一为 `留学`。
4. 原始标签仍保留在内容详情中，Tab 只使用归一后的展示标签。

搜索与 Tab 的关系：

1. 搜索只影响内容列表，不改变顶部 Tab 集合。
2. 用户在某个 Tab 内搜索时，只搜索当前 Tab 下内容。
3. 清空搜索后恢复当前 Tab 的内容列表。
4. 若搜索结果为空，展示搜索空态，但 Tab 不消失。

白盒卡片字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `post_id` | string | 笔记 ID |
| `title` | string | 标题 |
| `author_name` | string | 作者名 |
| `author_avatar` | string | 作者头像 |
| `author_seed` | string | 原型用头像种子，真实开发用头像 URL |
| `is_ai` | boolean | 是否 AI 自动生成，true 时展示 AI 标识 |
| `category` | string | 分类 |
| `tag` | string | 标签 |
| `likes_count` | integer | 点赞数 |
| `comments_count` | integer | 评论数 |
| `saves_count` | integer | 收藏数 |
| `views_count` | integer | 浏览数 |
| `published_at` | datetime | 发布时间 |
| `read_status` | boolean | 是否已读 |
| `visibility` | enum | `public` / `private` |
| `source` | enum | `manual` / `auto_whitebox` |

卡片展示规则：

1. 未读白盒为白色玻璃卡片。
2. 点击阅读后，该内容可按分类色显示为已读态。
3. AI 自动生成内容需展示 AI 标识。
4. 私密内容仅在作者自己的主页可见，首页公共流不展示。
5. 标题最多 3 行，超出省略。
6. 摘要不再展示。
7. 卡片背景、已读态颜色、打开后的展开底图不得在客户端写死，需读取后台视觉配置。

### 4.4 笔记详情页

字段：

| 字段 | 说明 |
|---|---|
| 标题 | 大字号正文标题 |
| 作者头像 | 点击进入作者名片详情 |
| 作者名 | 展示发布者 |
| 发布时间 | 展示相对时间或标准日期 |
| 关注按钮 | 关注/已关注 |
| 开聊按钮 | 进入与作者分身聊天 |
| 正文 | 按段落展示 |
| 点赞 | 支持点赞/取消点赞 |
| 评论 | 支持评论、回复、点赞评论 |
| 收藏 | 支持收藏/取消收藏 |
| 分享 | 支持分享给好友、保存图片、复制链接 |

交互：

1. 点击卡片进入详情，记录已读状态。
2. 点击作者头像进入作者名片。
3. 点击开聊进入聊天页。
4. 点击关注切换关注状态。
5. 评论输入为空时不可提交。
6. 点击分享弹出底部分享面板。

### 4.5 白盒展开颜色底图展示规则

白盒打开后，页面的颜色底图和已读态颜色卡图需由后台配置下发。客户端只负责渲染，不负责写死分类色或图片资源。

配置生效范围：

| 范围 | 说明 | 示例 |
|---|---|---|
| 全局默认 | 没命中任何配置时使用 | 默认紫色渐变 |
| 分类 | 按 `category` 生效 | 考研、实习、职场 |
| 标签 | 按 `tag` 生效 | 保研、产品面试 |
| 单篇内容 | 按 `post_id` 生效 | 指定某篇白盒 |

优先级：

`单篇内容配置 > 标签配置 > 分类配置 > 全局默认配置`

配置类型：

| 类型 | 字段 | 前台展示 |
|---|---|---|
| 纯色 | `solid_color` | 使用单一背景色 |
| 渐变色 | `gradient_colors` + `gradient_angle` | 使用线性渐变 |
| 颜色卡图 | `card_image_url` | 用于首页已读态/卡片背景 |
| 展开底图 | `detail_bg_image_url` | 用于白盒打开后的详情背景或头图区 |
| 稀有底图 | `rarity_level` + `weight` + 图片/颜色配置 | 用于打开白盒后的盲盒式视觉惊喜 |

展示规则：

1. 未读态仍以浅色白盒为主，弱化底图，仅展示干净白盒形态。
2. 用户点击打开后，该白盒进入已读态；返回首页时卡片可展示后台配置的颜色或颜色卡图。
3. 笔记详情页顶部或背景可展示后台配置的展开底图；正文阅读区仍需保证文字可读性。
4. 若配置为图片，图片加载失败时回退到同级配置的渐变色；仍失败则回退全局默认渐变。
5. 暗黑模式下优先使用暗黑专用底图；未配置暗黑图时使用暗黑模式渐变兜底，不允许直接强制反色图片。
6. 前台需对底图增加可读性遮罩，确保标题/作者/操作按钮对比度满足可读要求。
7. 白盒底图命中结果必须由服务端返回，客户端不得根据点击次数、阅读时长或本地随机数自行计算稀有概率。
8. 同一用户首次打开同一篇白盒后，服务端需绑定该用户对该内容的底图结果；同一用户再次打开同一篇内容时展示同一底图。
9. 用户点击白盒后，先展示命中的展开背景图或色块，停留 `500-800ms` 后再进入笔记详情页；若底图资源加载失败，需立即使用兜底渐变，整体等待时间仍控制在 `500-800ms`。

底图等级规则：

| 等级 | `rarity_level` | 默认命中概率 | 说明 |
|---|---|---:|---|
| 普通 | `normal` | 90% | 常规底图，用于大多数白盒内容 |
| 稀有 | `rare` | 10% | 稀有底图或稀有色块，用于制造轻量惊喜 |

稀有底图权重配置：

1. `normal` 与 `rare` 的大盘命中比例固定为 `90% / 10%`，服务端按用户打开白盒时实时命中。
2. `rare` 命中后，在稀有底图库内按运营配置的单图权重二次抽取。
3. 每张稀有底图或色块需支持独立配置 `weight`，权重范围 `1-1000`，默认 `100`。
4. 运营可在后台灵活新增、下线、调权稀有底图；客户端只渲染服务端返回结果。
5. 稀有底图可以是图片，也可以是颜色卡或渐变色块；均需配置浅色/深色可读方案。

盲盒体验规则：

1. 首页连续打开或阅读的 6 个白盒，主色不得完全相同；若后台命中相同或相近主色，服务端需在同等级可用底图库内重抽。
2. 相邻 6 个底图的相似度判断以 `main_color` 为准；同一色系可允许出现，但不得连续重复同一主色。
3. 重复打开、快速退出、频繁刷新，不提高稀有概率。
4. 点赞、收藏、评论、分享、阅读时长不提高稀有概率。
5. 用户不应看到明确倒计时、进度条或“再开几次必出”的提示，避免形成可被抓规律的机制。
6. 若稀有底图资源被下线，已绑定用户再次打开时需回退到其他稀有资源；没有可用稀有资源时回退普通底图。
7. 普通、稀有底图均需同时配置浅色与深色可读方案；缺少深色图时必须使用深色兜底渐变和遮罩。

行为埋点规则：

| 行为 | 统计用途 | 限制 |
|---|---|---|
| 有效阅读 | 内容质量、推荐排序、数据分析 | 不影响稀有概率 |
| 点赞 | 互动统计、推荐排序、数据分析 | 不影响稀有概率 |
| 收藏 | 互动统计、推荐排序、数据分析 | 不影响稀有概率 |
| 评论 | 互动统计、评论排序、数据分析 | 不影响稀有概率 |
| 分享 | 分享统计、数据分析 | 不影响稀有概率 |
| 快速退出 | 质量分析 | 不提高稀有概率 |
| 重复打开 | 使用首次绑定底图 | 不重复抽取 |

有效阅读阈值建议：白盒详情页停留时间 ≥ 8 秒，或正文滚动深度 ≥ 60%，满足其一即可计为有效阅读。有效阅读只用于内容质量、推荐排序和数据分析，不参与稀有底图概率计算。

前台渲染字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `visual_config_id` | string | 命中的视觉配置 ID |
| `rarity_level` | enum | `normal` / `rare` |
| `main_color` | string | 主色 HEX，用于相邻 6 个底图去重 |
| `visual_seed` | string | 服务端生成的稳定随机种子 |
| `is_first_open` | boolean | 是否为当前用户首次打开该白盒 |
| `visual_type` | enum | `solid` / `gradient` / `card_image` / `detail_image` |
| `solid_color` | string | HEX 色值 |
| `gradient_colors` | array | 渐变色数组 |
| `gradient_angle` | number | 渐变角度，单位度 |
| `card_image_url_light` | string | 浅色模式颜色卡图 |
| `card_image_url_dark` | string | 深色模式颜色卡图 |
| `detail_bg_image_url_light` | string | 浅色模式展开底图 |
| `detail_bg_image_url_dark` | string | 深色模式展开底图 |
| `overlay_opacity` | number | 遮罩透明度，取值 0-1 |
| `text_tone` | enum | `light` / `dark` / `auto` |

### 4.6 我的主页同步

自动发布成功后，需同步到「我的 → 外表 → 笔记」列表。

字段：

| 字段 | 说明 |
|---|---|
| 标题 | 自动生成笔记标题 |
| 摘要 | 自动生成摘要 |
| 来源标识 | `AI 自动生成` |
| 可见性 | 默认公开 |
| 编辑入口 | 支持编辑 |
| 权限入口 | 支持公开/不公开切换 |
| 删除入口 | 支持删除 |

规则：

1. 用户删除自动发布笔记，只删除该笔记，不影响后续自动发布授权。
2. 用户将笔记设为不公开，首页公共流立即下架。
3. 用户关闭自动发布后，历史已发布笔记保留。

### 4.7 笔记互动与数据展示

首页白盒笔记和笔记详情页需支持完整互动能力：分享、点赞、收藏、评论、回复、关注作者、开聊。

列表卡片展示字段：

| 字段 | 展示规则 |
|---|---|
| 点赞数 | 展示 `likes_count`，超过 999 按 `1.2k` 形式缩写 |
| 收藏数 | 展示 `saves_count`，超过 999 按 `1.2k` 形式缩写 |
| 评论数 | 展示 `comments_count` |
| 浏览数 | 详情页或我的主页可展示 `views_count` |
| 发布时间 | 展示相对时间，如 `2小时前`、`昨天` |
| AI 标识 | `is_ai_generated=true` 时展示 |

笔记详情页互动：

| 交互 | 规则 |
|---|---|
| 点赞 | 点击后切换点赞/取消点赞，前端可乐观更新，失败需回滚 |
| 收藏 | 点击后切换收藏/取消收藏，收藏成功可 Toast 提示 |
| 评论 | 输入非空才可提交，提交成功后插入评论列表 |
| 回复 | 支持回复指定评论，显示 `回复 用户名` |
| 评论点赞 | 支持点赞/取消点赞评论和回复 |
| 分享 | 支持分享给好友、保存图片、复制链接 |
| 关注作者 | 支持关注/取消关注作者，状态同步到名片页 |
| 开聊 | 进入与作者分身聊天页 |

计数更新规则：

1. 点赞、收藏、评论提交后，前端可先乐观更新本地计数。
2. 接口失败时需回滚本地计数并提示失败。
3. 最终以服务端返回计数为准。
4. 同一用户对同一笔记只能点赞 1 次、收藏 1 次。
5. 删除评论后评论数同步减少。
6. 分享动作默认只记录分享行为，不增加点赞/收藏/评论数。
7. 保存图片、复制链接、分享给好友均需记录分享埋点。

评论区分页与排序规则：

1. 首页笔记评论默认使用综合排序，不按单纯时间倒序。
2. 置顶优先：只有发布笔记的作者可置顶评论，置顶评论强制置于评论列表最顶部，不受热度、发布时间影响；单篇笔记最多支持 1 条置顶评论。
3. 更换置顶：若当前笔记已有置顶评论，作者再选择其他评论置顶时，需弹窗提示「更换置顶评论？继续后将取消当前置顶并置顶新的评论」，确认后取消原置顶并置顶新评论，取消则保持原状态。
4. 评论项操作入口：不使用 `...` 作为主要入口；点击评论条目弹出评论项操作面板，按权限展示「置顶评论 / 取消置顶 / 删除评论」。
5. 主楼评论权重计算：非置顶一级评论按综合权重排序，核心因子为评论点赞数量、楼中楼回复条数；作者回复过的评论、带图评论、完整长内容评论获得权重加成；权重接近时，发布时间较新的评论排序靠前。
6. 新评论临时上浮：评论刚提交成功后，可在当前用户本地临时上浮展示；页面刷新或重新拉取评论列表后，回归服务端综合权重排序。
7. 楼中楼子回复：子回复内部按发布时间倒序展示；子回复不单独参与主楼权重打分，但子回复数量会提升对应主楼评论的权重分值。
8. 降权下沉逻辑：广告导流、灌水重复、低质无意义内容需被算法降权；被较多用户点击不喜欢、被有效举报的评论会进一步下沉，严重时直接隐藏且不对外展示。
9. 笔记预览区展示：笔记正文下方露出的少量评论，取自默认综合排序结果。
10. 一级评论分页加载，默认每页 20 条。
11. 每条一级评论默认展示最多 3 条回复。
12. 回复超过 3 条时展示「展开更多回复」，点击后分页加载该评论下的回复。
13. 已删除评论展示为「该评论已删除」，其子回复保留展示。
14. 被平台隐藏的评论不向普通用户展示，但作者本人可见弱提示「该评论仅自己可见」。
15. 评论提交、回复提交成功后，若命中审核延迟，则先显示「审核中」状态。

分享成功定义：

| 分享动作 | 成功判定 | 成功反馈 |
|---|---|---|
| 分享给好友 | 系统分享面板成功唤起并返回成功回调；无回调平台按点击分享按钮记录 | Toast：已打开分享 |
| 保存图片 | 图片生成成功并保存到相册/本地；若无权限需提示开启权限 | Toast：图片已保存 |
| 复制链接 | 链接写入剪贴板成功 | Toast：链接已复制 |

分享埋点口径：

1. 点击分享入口记录 `share_panel_open`。
2. 点击具体分享动作记录 `share_action_click`。
3. 分享成功记录 `share_success`。
4. 分享失败记录 `share_failed`，需带失败原因。
5. 分享成功不改变点赞、收藏、评论、浏览计数。

### 4.8 名片数据统计

名片页需展示关注、粉丝、获赞与收藏、互动 4 个统计项。原型展示位置位于名片信息区底部，四项横向均分。

字段：

| 展示名称 | 字段 | 统计口径 |
|---|---|---|
| 关注 | `following_count` | 当前用户/分身主动关注的用户或分身数量 |
| 粉丝 | `followers_count` | 关注当前用户/分身的人数 |
| 获赞与收藏 | `likes_saves_count` | 当前用户公开内容收到的点赞数 + 收藏数 |
| 互动 | `interaction_count` | 当前用户/分身被点击开聊并成功进入聊天页的次数 |

互动口径：

1. 一期 `interaction_count` 只统计开聊次数。
2. 用户点击「开聊」并成功进入聊天页后，计为 1 次互动。
3. 每点击并成功进入一次聊天页，计数 +1；客户端需做防重复点击，避免一次点击触发多次计数。
4. 「互动」统计项只展示数字，不可点击，不进入明细页。
5. 后续可扩展为：开聊次数 + 评论回复数 + 咨询下单/邀约次数。
6. 若后续扩展口径，需通过 `stats_version` 标记统计版本，不得静默改变线上展示口径。

获赞与收藏口径：

1. 只统计公开内容。
2. 包含手动发布笔记和自动白盒笔记。
3. 不公开、删除、审核下架内容不计入。
4. 内容重新公开后可重新计入。

展示规则：

1. 数值为 0 时展示 `0`。
2. 1000 以上展示 1 位小数，如 `3.2k`。
3. 10000 以上仍可展示 `12.4k`，不展示完整长数字。
4. 点击「关注」进入关注列表。
5. 点击「粉丝」进入粉丝列表。
6. 点击「获赞与收藏」进入数据统计明细页。
7. 「互动」不可点击，只展示计数。
8. 关注按钮状态需与粉丝数联动：关注成功后对方 `followers_count +1`，取消关注后 `followers_count -1`。

关注列表：

| 字段 | 说明 |
|---|---|
| 用户头像 | 点击进入该用户名片详情页 |
| 用户名 | 展示用户/分身名称 |
| 简介 | 展示一句简介或身份信息 |
| 关注状态 | 已关注/互相关注/关注按钮 |
| 最近互动时间 | 可选，展示最近关注或互动时间 |

交互规则：

1. 点击列表用户头像或整行，进入该用户名片详情页。
2. 支持分页加载，默认每页 20 条。
3. 空态文案：`暂无关注`。
4. 当前用户查看自己的关注列表时，可取消关注。
5. 查看他人关注列表时，是否可见由隐私配置决定；一期默认公开。

粉丝列表：

| 字段 | 说明 |
|---|---|
| 粉丝头像 | 点击进入粉丝名片详情页 |
| 粉丝名 | 展示粉丝名称 |
| 简介 | 展示一句简介或身份信息 |
| 关注状态 | 未关注/已关注/互相关注 |
| 关注时间 | 粉丝关注当前用户的时间 |

交互规则：

1. 点击粉丝头像或整行，进入粉丝名片详情页。
2. 支持分页加载，默认每页 20 条。
3. 空态文案：`暂无粉丝`。
4. 当前用户可在粉丝列表中回关或取消关注。

获赞与收藏明细：

展示形态参考原型「我的 → 外表 → 获赞与收藏」的数据统计样式，以统计项列表为主，不做复杂图表。

字段：

| 字段 | 说明 |
|---|---|
| 统计项名称 | 如当前分身获得赞数、当前笔记获得收藏数、评论被点赞 |
| 数值 | 展示该项累计数量 |
| 来源说明 | 展示来源内容或场景 |
| 更新时间 | 可选，展示最近统计时间 |

明细分类：

1. 当前分身获得赞数。
2. 当前笔记获得赞数。
3. 当前笔记获得收藏数。
4. 当前我的评论获赞数。
5. 分身主页被收藏。
6. 评论被点赞。

刷新策略：

1. 名片页进入时请求最新统计。
2. 关注/取消关注成功后立即更新关注状态和粉丝数。
3. 点赞、收藏、评论、开聊成功后，相关计数可乐观更新。
4. 接口失败时回滚本地计数，并展示弱提示。
5. 统计缓存时长 5 分钟；用户下拉刷新或重新进入页面需重新请求。
6. 聚合统计允许异步延迟，但最终一致延迟不得超过 5 分钟。

私密、删除、封禁对统计的影响：

| 场景 | 统计处理 |
|---|---|
| 笔记设为不公开 | 从获赞与收藏统计中扣减 |
| 笔记重新公开 | 重新计入获赞与收藏统计 |
| 笔记删除 | 从获赞与收藏统计中扣减 |
| 笔记审核下架 | 从公开统计中扣减 |
| 用户封禁 | 名片统计对外隐藏或展示为不可用 |
| 取消关注 | 关注方 `following_count -1`，被关注方 `followers_count -1` |
| 删除评论 | 评论获赞从获赞与收藏明细中扣减 |

### 4.9 我的外表列表排序规则

「我的 → 外表」内的笔记、评论、收藏属于用户个人内容管理列表，排序规则与首页笔记评论区不同，统一按时间倒序展示。

| 列表 | 排序口径 | 展示规则 |
|---|---|---|
| 我的-外表-笔记 | 按笔记发布时间/发布成功时间倒序 | 新发布的笔记在最前 |
| 我的-外表-评论 | 按评论发布时间倒序 | 新发表的评论在最前 |
| 我的-外表-收藏 | 按收藏时间倒序 | 新收藏的内容在最前 |

补充规则：

1. 我的-外表-笔记、评论、收藏不使用首页评论默认综合排序。
2. 编辑可见性、删除、取消收藏后，当前列表需即时刷新或本地移除对应条目。
3. 若服务端分页返回存在同一时间戳数据，按内容 ID 倒序作为兜底排序，保证翻页稳定不重复。

## 5. 自动发布业务规则

### 5.1 后台知识库分类

本期新增后台知识库分类能力。该分类属于服务端内容资产管理能力，用于让平台按分类组织用户知识库关键词，并作为白盒自动生成笔记的输入条件之一。前台「咨询服务」本期整体隐藏，不参与本期开发。

字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `category_id` | string | 分类 ID |
| `category_name` | string | 分类名称 |
| `sort_order` | int | 排序值，越小越靠前 |
| `enabled` | boolean | 是否启用 |
| `keyword_count` | int | 分类下关键词数量 |
| `updated_at` | datetime | 最近更新时间 |

规则：

1. 后台需支持分类创建、编辑、删除、启停和排序。
2. 白盒自动生成读取知识库关键词时，需带上分类信息，便于后续运营和推荐策略使用。
3. 未归类关键词统一进入默认分类 `未分类`。
4. 分类停用后，不再作为新白盒生成的优先输入；历史已生成笔记不受影响。
5. 本期客户端只展示服务端返回结果，不在本地维护知识库分类规则。

### 5.2 授权规则

只有满足以下条件的用户才进入自动发布候选池：

1. 用户状态正常。
2. 自动发布授权状态为 `enabled`。
3. 用户满足内容生成硬性门槛：至少填写过 1 个名片标签，或后台知识库存在至少 1 条可用于生成内容的数据。
4. 用户未被平台风控限制发文。
5. 当前自然周未超过 3 篇自动发布配额。

硬性门槛说明：

1. 用户只同意授权但没有名片标签、也没有后台知识库内容时，不进入自动发布候选池，不生成任务、不自动发文。
2. 用户后续补充名片标签，或后台新增可用知识库内容后，系统需在下一次候选池扫描时将其纳入自动发布列表。
3. 候选池扫描建议至少每日执行 1 次；若用户补充内容后当前自然周仍有可用日期和时间段，可按周中授权补生成逻辑补任务。
4. 设置页可展示状态提示：`补充名片标签或知识库后，自动发布才会生效`。

### 5.3 周配额规则

1. 每个授权用户每自然周最多自动发布 3 篇。
2. 自然周按北京时间 `Asia/Shanghai` 计算。
3. 每周一 `00:00:00` 重置周配额。
4. 周配额只统计 `source=auto_whitebox` 且最终成功发布的笔记。
5. 已预生成但未发布的任务占用预留配额；若任务取消或顺延，可释放当周预留配额。

### 5.4 可发送时间段

| 时间段 | 代码 | 开始时间 | 结束时间 | 规则 |
|---|---|---:|---:|---|
| S1 | `S1` | 06:00:00 | 08:00:00 | 含开始，不含结束 |
| S2 | `S2` | 08:00:00 | 10:00:00 | 含开始，不含结束 |
| S3 | `S3` | 12:00:00 | 14:00:00 | 含开始，不含结束 |
| S4 | `S4` | 17:00:00 | 23:00:00 | 含开始，不含结束 |

非以上时间段禁止自动发布。

### 5.5 两个强制约束

同一用户同一自然周内，必须同时满足：

1. 3 次推送使用 3 个不同时间段，时间段不能重复。
2. 3 次推送分配到 3 个不同自然日，同一天不能给同一用户发多条。

示例：

| 是否合法 | 日期 | 时间段 |
|---|---|---|
| 合法 | 周二 S1、周四 S3、周日 S4 | 3 个不同日期 + 3 个不同时间段 |
| 不合法 | 周二 S1、周二 S3、周五 S4 | 同一天重复 |
| 不合法 | 周二 S1、周四 S1、周日 S4 | 时间段重复 |

### 5.6 任务生成规则

常规定时：

1. 每周一 `00:00:00` 预生成本周全部自动发布任务。
2. 任务生成对象：授权状态为 `enabled`，且已满足「至少 1 个名片标签或至少 1 条后台知识库内容」硬性门槛的用户。
3. 每个用户最多生成 3 条任务。
4. 每条任务绑定：
   - `execution_date`
   - `slot_code`
   - `slot_start_at`
   - `slot_end_at`
   - `scheduled_at`

周中首次授权：

1. 若用户在周一 00:00 后首次允许授权，需立即为当前周补生成任务。
2. 补生成只能使用当前时间之后的自然日和时间段。
3. 若当前周剩余合法日期或时间段不足 3 个，则能生成几条生成几条；未生成的配额不强行补发，顺延到下一自然周。

`scheduled_at` 生成建议：

1. `scheduled_at` 必须落在对应时间段内。
2. 为避免所有任务整点集中发布，可在时间段内做稳定随机分布。
3. 稳定随机种子建议使用：`hash(user_id + week_start_date + slot_code)`。
4. S1/S2/S3 建议发布时间落在窗口开始后 5 分钟至窗口结束前 5 分钟。
5. S4 建议发布时间落在 17:10:00 至 22:50:00。

### 5.7 任务分配算法

输入：

1. `user_id`
2. `week_start_date`
3. 当前周已用日期集合 `used_dates`
4. 当前周已用时间段集合 `used_slots`
5. 当前可用日期集合
6. 当前可用时间段集合

输出：

最多 3 条满足约束的任务。

分配步骤：

1. 读取用户本周已用日期和已用时间段。
2. 过滤掉已用日期。
3. 过滤掉已用时间段。
4. 从剩余日期中选择最多 3 个不同日期。
5. 从剩余时间段中选择相同数量的不同时间段。
6. 为每个日期绑定一个时间段。
7. 为每条任务生成 `scheduled_at`。
8. 写入任务表，状态为 `scheduled`。

推荐分配策略：

1. 日期尽量分散，避免连续 3 天集中发布。
2. 时间段尽量轮换，避免大量用户集中在同一窗口。
3. 如果平台需要控流，可按用户 ID hash 分桶生成。

伪代码：

```text
slots = [S1, S2, S3, S4] - used_slots
dates = week_dates_after_now - used_dates

target_count = min(3 - sent_count - reserved_count, slots.length, dates.length)
if target_count <= 0:
  return []

selected_dates = spread_pick(dates, target_count, seed=user_id + week_start)
selected_slots = stable_shuffle(slots, seed=user_id + week_start).take(target_count)

for i in range(target_count):
  create_task(user_id, selected_dates[i], selected_slots[i])
```

### 5.8 发送失败与重试

发送失败定义：

1. 内容生成失败。
2. 内容审核失败。
3. 发布接口失败。
4. 数据库写入失败。
5. 任务执行超时。

重试规则：

1. 发送失败后只能在原时间窗口内重试。
2. 原时间窗口内最多重试 3 次。
3. 建议重试间隔：5 分钟、15 分钟、30 分钟。
4. 若重试时当前时间已超过 `slot_end_at`，停止本周重试。
5. 窗口错过后，本次推送顺延下一周，不得在非允许时间段补发。
6. 顺延任务进入下一周分配池，仍需满足下一周的不同日期、不同时间段规则。

### 5.9 任务取消规则

以下情况需取消未执行任务：

1. 用户关闭「AI 智库自动发文」。
2. 用户撤回授权。
3. 用户账号注销、封禁、冻结。
4. 用户知识库被清空或不可用。
5. 平台风控判定不可发布。

取消后：

1. 未执行任务状态改为 `cancelled`。
2. 已发布内容不自动删除。
3. 任务取消需记录原因。

## 6. 数据结构建议

### 6.1 自动发布授权表

表名：`auto_publish_authorizations`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | bigint | 主键 |
| `user_id` | bigint | 用户 ID |
| `status` | enum | `unconfirmed` / `enabled` / `denied` / `disabled_by_user` / `disabled_by_platform` |
| `consent_version` | string | 授权协议版本 |
| `consent_at` | datetime | 同意时间 |
| `denied_at` | datetime | 拒绝时间 |
| `disabled_at` | datetime | 关闭时间 |
| `source` | enum | `home_modal` / `settings_toggle` / `system_migration` |
| `timezone` | string | 默认 `Asia/Shanghai` |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

唯一索引：`user_id + consent_version`

### 6.2 周配额表

表名：`auto_publish_weekly_usage`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | bigint | 主键 |
| `user_id` | bigint | 用户 ID |
| `week_start_date` | date | 自然周周一日期 |
| `sent_count` | int | 已发布成功数量 |
| `reserved_count` | int | 已预留未执行数量 |
| `used_slots` | json | 已使用时间段，如 `["S1","S3"]` |
| `used_dates` | json | 已使用日期，如 `["2026-08-03"]` |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

唯一索引：`user_id + week_start_date`

### 6.3 自动发布任务表

表名：`auto_publish_tasks`

| 字段 | 类型 | 说明 |
|---|---|---|
| `task_id` | bigint | 主键 |
| `user_id` | bigint | 用户 ID |
| `week_start_date` | date | 所属自然周 |
| `execution_date` | date | 执行日期 |
| `slot_code` | enum | `S1` / `S2` / `S3` / `S4` |
| `slot_start_at` | datetime | 窗口开始时间 |
| `slot_end_at` | datetime | 窗口结束时间 |
| `scheduled_at` | datetime | 计划执行时间 |
| `status` | enum | `scheduled` / `generating` / `reviewing` / `publishing` / `sent` / `retry_pending` / `failed` / `window_missed` / `deferred_next_week` / `cancelled` |
| `retry_count` | int | 重试次数 |
| `source_type` | enum | `profile_tag` / `knowledge_item` | 本次生成使用的核心素材类型 |
| `source_id` | string | 本次生成使用的名片标签 ID 或知识库内容 ID |
| `knowledge_snapshot_id` | bigint | 知识库快照 ID；当 `source_type=knowledge_item` 时必填 |
| `generated_content_id` | bigint | 生成内容 ID |
| `post_id` | bigint | 发布成功后的笔记 ID |
| `fail_reason` | string | 失败原因 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

唯一索引：`user_id + week_start_date + execution_date`  
唯一索引：`user_id + week_start_date + slot_code`

### 6.4 笔记表扩展字段

表名：`posts`

| 字段 | 类型 | 说明 |
|---|---|---|
| `source` | enum | `manual` / `auto_whitebox` |
| `auto_publish_task_id` | bigint | 自动发布任务 ID |
| `is_ai_generated` | boolean | 是否 AI 生成 |
| `visibility` | enum | `public` / `private` |
| `published_at` | datetime | 发布时间 |
| `category` | string | 分类 |
| `tag` | string | 标签 |
| `visual_config_id` | bigint | 命中的白盒视觉配置 ID |

### 6.5 白盒视觉配置表

表名：`whitebox_visual_configs`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | bigint | 主键 |
| `config_name` | string | 配置名称，运营可识别 |
| `scope_type` | enum | `global` / `category` / `tag` / `post` |
| `scope_value` | string | 作用对象，如 `考研`、`post_id`；全局配置为空 |
| `visual_type` | enum | `solid` / `gradient` / `card_image` / `detail_image` |
| `rarity_level` | enum | `normal` / `rare`，默认 `normal` |
| `weight` | int | 稀有底图库内命中权重，默认 100；普通底图可不参与权重抽取 |
| `main_color` | string | 主色 HEX，用于相邻 6 个白盒去重 |
| `solid_color` | string | 纯色 HEX，如 `#9F97EB` |
| `gradient_angle` | int | 渐变角度，0-360 |
| `gradient_colors` | json | 渐变色数组 |
| `card_image_url_light` | string | 浅色模式颜色卡图 |
| `card_image_url_dark` | string | 深色模式颜色卡图 |
| `detail_bg_image_url_light` | string | 浅色模式展开底图 |
| `detail_bg_image_url_dark` | string | 深色模式展开底图 |
| `overlay_opacity` | decimal | 遮罩透明度，0-1 |
| `text_tone` | enum | `light` / `dark` / `auto` |
| `priority` | int | 同范围下的优先级，数字越大越优先 |
| `status` | enum | `draft` / `published` / `offline` |
| `published_at` | datetime | 发布时间 |
| `published_by` | bigint | 发布人 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

唯一约束建议：同一 `scope_type + scope_value` 只能有 1 条 `published` 状态配置。

### 6.5.1 白盒用户底图命中记录表

表名：`whitebox_user_visual_assignments`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | bigint | 主键 |
| `user_id` | bigint | 用户 ID |
| `post_id` | bigint | 白盒笔记 ID |
| `visual_config_id` | bigint | 命中的视觉配置 ID |
| `rarity_level` | enum | `normal` / `rare` |
| `main_color` | string | 主色 HEX |
| `visual_seed` | string | 稳定随机种子 |
| `is_first_open` | boolean | 首次打开时为 true |
| `effective_read_counted` | boolean | 是否已计入有效阅读统计，不影响稀有概率 |
| `created_at` | datetime | 首次命中时间 |
| `updated_at` | datetime | 更新时间 |

唯一索引：`user_id + post_id`

用途：

1. 保证同一用户重复打开同一白盒时底图一致。
2. 支持排查稀有底图命中率和异常回退。
3. 支持相邻 6 个白盒主色去重时读取用户最近打开记录。
4. 防止用户通过刷新或重复打开刷高稀有概率。

### 6.6 白盒视觉配置版本表

表名：`whitebox_visual_config_versions`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | bigint | 主键 |
| `config_id` | bigint | 配置 ID |
| `version_no` | int | 版本号 |
| `snapshot` | json | 配置快照 |
| `operator_id` | bigint | 操作人 |
| `operation` | enum | `create` / `update` / `publish` / `rollback` / `offline` |
| `created_at` | datetime | 创建时间 |

### 6.7 白盒 Tab 配置表

表名：`whitebox_tab_configs`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | bigint | 主键 |
| `tab_key` | string | Tab 唯一标识，如 `all`、`career` |
| `tab_label` | string | 展示名 |
| `source_type` | enum | `category` / `tag` / `manual` / `alias` |
| `source_value` | string | 原始来源值 |
| `sort_order` | int | 排序值，越小越靠前 |
| `pin` | boolean | 是否置顶 |
| `min_post_count` | int | 最少可展示内容数，默认 1 |
| `status` | enum | `enabled` / `disabled` |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

### 6.8 白盒 Tab 映射表

表名：`whitebox_tab_aliases`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | bigint | 主键 |
| `alias_label` | string | 别名，如 `科普`、`留学申请` |
| `canonical_tab_key` | string | 归一后的 Tab key |
| `canonical_tab_label` | string | 归一后的 Tab 名称 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

### 6.9 笔记互动表

表名：`post_interactions`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | bigint | 主键 |
| `post_id` | bigint | 笔记 ID |
| `user_id` | bigint | 操作用户 |
| `interaction_type` | enum | `like` / `save` / `share` / `view` |
| `share_channel` | enum | `friend` / `image` / `link`，仅分享时有值 |
| `status` | enum | `active` / `cancelled` |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

唯一约束：

1. `post_id + user_id + interaction_type=like` 同一时间只能有 1 条 `active`。
2. `post_id + user_id + interaction_type=save` 同一时间只能有 1 条 `active`。
3. 分享和浏览可多次记录。

### 6.10 评论表

表名：`post_comments`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | bigint | 主键 |
| `post_id` | bigint | 笔记 ID |
| `user_id` | bigint | 评论用户 |
| `parent_comment_id` | bigint | 父评论 ID，一级评论为空 |
| `reply_to_user_id` | bigint | 被回复用户 |
| `content` | string | 评论内容 |
| `likes_count` | int | 评论点赞数 |
| `status` | enum | `visible` / `deleted` / `hidden` |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

### 6.11 关注关系表

表名：`user_follows`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | bigint | 主键 |
| `follower_user_id` | bigint | 关注发起方 |
| `target_user_id` | bigint | 被关注用户/分身 |
| `status` | enum | `following` / `cancelled` |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

唯一约束：`follower_user_id + target_user_id`

### 6.12 名片统计聚合表

表名：`profile_stats`

| 字段 | 类型 | 说明 |
|---|---|---|
| `profile_user_id` | bigint | 用户/分身 ID |
| `following_count` | int | 关注数 |
| `followers_count` | int | 粉丝数 |
| `likes_count` | int | 获赞数 |
| `saves_count` | int | 获收藏数 |
| `likes_saves_count` | int | 获赞与收藏 |
| `interaction_count` | int | 有效互动数 |
| `stats_version` | string | 统计口径版本 |
| `updated_at` | datetime | 更新时间 |

统计更新：

1. 点赞/取消点赞、收藏/取消收藏、公开/不公开、删除/恢复时需更新 `likes_saves_count`。
2. 关注/取消关注时需更新 `following_count` 和 `followers_count`。
3. `/chat-open` 记录成功后需更新 `interaction_count`。
4. 聚合表可异步更新，但前台最终展示延迟不得超过 5 分钟。

## 7. 接口需求

### 7.1 查询授权状态

`GET /api/v1/whitebox/auto-publish/authorization`

返回：

```json
{
  "status": "enabled",
  "consentVersion": "2026-08-06",
  "shouldShowHomeModal": false,
  "weeklyQuota": 3,
  "usedCountThisWeek": 1,
  "nextScheduledAt": "2026-08-07T08:36:00+08:00"
}
```

### 7.2 更新授权状态

`POST /api/v1/whitebox/auto-publish/authorization`

请求：

```json
{
  "action": "allow",
  "source": "home_modal",
  "consentVersion": "2026-08-06"
}
```

`action` 枚举：

| action | 说明 |
|---|---|
| `allow` | 允许自动发布 |
| `deny` | 暂不允许 |
| `disable` | 用户关闭 |
| `enable` | 用户重新开启 |

返回：

```json
{
  "status": "enabled",
  "generatedTaskCount": 2,
  "message": "已开启自动发布"
}
```

### 7.3 首页白盒列表

`GET /api/v1/home/whitebox-posts`

参数：

| 参数 | 类型 | 说明 |
|---|---|---|
| `tab` | string | 分类，默认 `全部` |
| `keyword` | string | 搜索关键词 |
| `cursor` | string | 分页游标 |
| `pageSize` | int | 默认 20，最大 50 |

返回字段需包含 4.3 白盒卡片字段，并包含当前卡片命中的 `visual_config`。

### 7.4 首页白盒 Tab 列表

`GET /api/v1/home/whitebox-tabs`

参数：

| 参数 | 类型 | 说明 |
|---|---|---|
| `keyword` | string | 可选，默认不传 |
| `scope` | string | 可选，`public` / `my` / `all` |

返回：

| 字段 | 类型 | 说明 |
|---|---|---|
| `tab_key` | string | Tab 唯一标识 |
| `tab_label` | string | Tab 展示名 |
| `count` | int | 当前可展示内容数量 |
| `pin` | boolean | 是否置顶 |
| `sort_order` | int | 排序值 |
| `source_type` | enum | `category` / `tag` / `manual` / `alias` |
| `source_value` | string | 原始来源值 |
| `is_default` | boolean | 是否为 `全部` |

返回示例：

```json
{
  "tabs": [
    {"tabKey":"all","tabLabel":"全部","count":128,"isDefault":true},
    {"tabKey":"career","tabLabel":"职场","count":22,"pin":true},
    {"tabKey":"internship","tabLabel":"实习","count":15,"pin":false}
  ]
}
```

### 7.5 笔记详情

`GET /api/v1/posts/{postId}`

返回字段需包含 4.4 详情页字段，并包含当前内容命中的 `visual_config`。

### 7.6 互动接口

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/v1/posts/{postId}/like` | POST/DELETE | 点赞/取消点赞 |
| `/api/v1/posts/{postId}/save` | POST/DELETE | 收藏/取消收藏 |
| `/api/v1/posts/{postId}/comments` | GET/POST | 评论列表/发表评论 |
| `/api/v1/comments/{commentId}/reply` | POST | 回复评论 |
| `/api/v1/comments/{commentId}/like` | POST/DELETE | 评论点赞/取消点赞 |
| `/api/v1/posts/{postId}/visibility` | PATCH | 公开/不公开 |
| `/api/v1/posts/{postId}` | DELETE | 删除笔记 |
| `/api/v1/posts/{postId}/share` | POST | 记录分享行为 |

互动接口返回要求：

1. 点赞、收藏、评论、删除、公开状态变更后，接口需返回最新计数。
2. 客户端乐观更新失败时，以接口错误回滚本地状态。
3. 分享接口需记录分享渠道，不改变点赞、收藏、评论计数。
4. 评论列表接口需支持 `cursor`、`pageSize`、`sort` 参数，首页笔记评论默认 `sort=comprehensive`。
5. 评论列表默认返回一级评论，每条评论附带最多 3 条回复预览。
6. 回复列表通过 `GET /api/v1/comments/{commentId}/replies` 分页获取。
7. 我的-外表-笔记、评论、收藏列表接口默认使用 `sort=time_desc`，不得套用首页评论综合排序。

评论列表返回字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `comment_id` | string | 评论 ID |
| `post_id` | string | 笔记 ID |
| `user_id` | string | 评论用户 ID |
| `user_name` | string | 评论用户昵称 |
| `user_avatar` | string | 评论用户头像 |
| `content` | string | 评论内容 |
| `likes_count` | int | 评论点赞数 |
| `liked_by_me` | boolean | 当前用户是否点赞 |
| `reply_count` | int | 回复数量 |
| `reply_preview` | array | 最多 3 条回复预览 |
| `status` | enum | `visible` / `reviewing` / `deleted` / `hidden` |
| `created_at` | datetime | 创建时间 |

### 7.7 名片关注与统计接口

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/v1/users/{userId}/follow` | POST/DELETE | 关注/取消关注 |
| `/api/v1/users/{userId}/chat-open` | POST | 记录点击开聊并成功进入聊天页 |
| `/api/v1/users/{userId}/profile-stats` | GET | 查询名片统计数据 |
| `/api/v1/users/{userId}/followers` | GET | 粉丝列表 |
| `/api/v1/users/{userId}/following` | GET | 关注列表 |
| `/api/v1/users/{userId}/received-likes-saves` | GET | 获赞与收藏明细 |

`GET /api/v1/users/{userId}/profile-stats` 返回：

```json
{
  "userId": 101,
  "followingCount": 0,
  "followersCount": 3200,
  "likesSavesCount": 2400,
  "interactionCount": 1200,
  "isFollowedByMe": false,
  "statsVersion": "chat_open_v1",
  "updatedAt": "2026-08-06T12:00:00+08:00"
}
```

统计接口要求：

1. 名片页进入时必须请求最新统计。
2. 关注/取消关注成功后需返回最新 `followersCount` 和 `isFollowedByMe`。
3. 若统计聚合延迟，服务端可先返回实时增量后的展示值，后台异步校准。
4. 统计接口失败时，名片页可展示缓存值，但需弱提示刷新失败。
5. `interactionCount` 一期只由 `/chat-open` 成功记录累加。
6. 「互动」不提供前台明细页，不允许点击跳转。

关注/粉丝列表返回字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `user_id` | string | 用户/分身 ID |
| `user_name` | string | 用户/分身名称 |
| `avatar_url` | string | 头像 |
| `bio` | string | 简介 |
| `identity` | string | 身份信息 |
| `is_followed_by_me` | boolean | 我是否已关注 |
| `is_mutual_follow` | boolean | 是否互相关注 |
| `followed_at` | datetime | 关注时间 |

获赞与收藏明细返回字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `metric_key` | string | 统计项标识 |
| `metric_label` | string | 展示名称 |
| `value` | int | 数值 |
| `source_title` | string | 来源内容标题或场景 |
| `source_type` | enum | `post` / `avatar` / `comment` |
| `source_id` | string | 来源 ID |
| `updated_at` | datetime | 更新时间 |

### 7.8 运营后台视觉配置接口

后台接口只供运营后台使用，客户端不直接调用编辑接口。

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/admin/v1/whitebox/visual-configs` | GET | 配置列表 |
| `/api/admin/v1/whitebox/visual-configs` | POST | 新建配置草稿 |
| `/api/admin/v1/whitebox/visual-configs/{id}` | GET | 配置详情 |
| `/api/admin/v1/whitebox/visual-configs/{id}` | PATCH | 编辑配置草稿 |
| `/api/admin/v1/whitebox/visual-configs/{id}/preview` | POST | 生成预览 |
| `/api/admin/v1/whitebox/visual-configs/{id}/publish` | POST | 发布配置 |
| `/api/admin/v1/whitebox/visual-configs/{id}/offline` | POST | 下线配置 |
| `/api/admin/v1/whitebox/visual-configs/{id}/rollback` | POST | 回滚到上一版本 |
| `/api/admin/v1/assets/upload` | POST | 上传颜色卡图/展开底图 |

客户端获取视觉配置：

1. 首页白盒列表接口直接返回命中的 `visual_config`，客户端无需再次请求。
2. 笔记详情接口直接返回命中的 `visual_config`。
3. 客户端需支持配置缓存，缓存时长 10 分钟；发布新配置后服务端可通过配置版本号让客户端刷新缓存。

## 8. 后台任务状态机

状态流转：

```text
scheduled
  -> generating
  -> reviewing
  -> publishing
  -> sent

generating/reviewing/publishing
  -> retry_pending
  -> generating/reviewing/publishing

retry_pending
  -> window_missed
  -> deferred_next_week

scheduled/retry_pending
  -> cancelled
```

状态说明：

| 状态 | 说明 |
|---|---|
| `scheduled` | 已生成，等待执行 |
| `generating` | 正在依据名片标签或后台知识库生成内容 |
| `reviewing` | 内容审核中 |
| `publishing` | 正在发布 |
| `sent` | 发布成功 |
| `retry_pending` | 失败后等待窗口内重试 |
| `failed` | 不可恢复失败 |
| `window_missed` | 已错过原时间窗口 |
| `deferred_next_week` | 顺延下一周 |
| `cancelled` | 已取消 |

## 9. 内容生成与审核要求

生成输入：

1. 用户名片标签。
2. 用户后台知识库内容。
3. 用户公开资料。
4. 用户过往公开笔记。
5. 用户授权状态。
6. 平台内容分类策略。

单次生成取材规则：

1. 每次自动生成笔记时，只调取 1 条名片标签，或 1 条后台知识库内容作为本次核心素材。
2. AI 需围绕该核心素材扩展生成完整笔记，可补充结构、案例、经验和建议，但不得脱离素材主题。
3. 不同批次笔记可以属于同一类型或同一分类，但笔记标题、正文核心观点和表达内容不得重复。
4. 服务端需记录本次使用的素材类型和素材 ID，便于去重、追溯和后续推荐。
5. 若同一用户可用标签和知识库内容较少，生成策略应优先做内容角度变化，而不是重复发布相似笔记。

生成输出：

| 字段 | 规则 |
|---|---|
| 标题 | 8-32 个中文字符，禁止标题党、违法违规词 |
| 摘要 | 40-100 个中文字符 |
| 正文 | 建议 500-1200 个中文字符 |
| 分类 | 必须命中平台分类配置 |
| 标签 | 1-3 个 |
| 作者 | 当前用户 |
| 来源 | `auto_whitebox` |

审核：

1. 发布前必须经过机审。
2. 机审不通过则不发布，并在原窗口内尝试重新生成。
3. 连续 3 次机审不通过，任务状态为 `failed`，本次不发布。
4. 涉及医疗、法律、金融等高风险内容需进入人工审核或禁止自动发布。

## 10. 异常与边界

| 场景 | 处理 |
|---|---|
| 用户无名片标签且无后台知识库 | 不进入自动发布候选池，不生成任务；设置页可展示「补充名片标签或知识库后生效」 |
| 用户只有名片标签、无后台知识库 | 可以进入自动发布候选池，AI 基于单条标签扩展生成笔记 |
| 用户只有后台知识库、无名片标签 | 可以进入自动发布候选池，AI 基于单条知识库内容扩展生成笔记 |
| 授权后当周剩余合法窗口不足 | 只生成可用数量，剩余不补发 |
| 发布时用户关闭授权 | 当前任务取消，不发布 |
| 发布成功后用户关闭授权 | 历史内容保留，后续不再发布 |
| 同一用户已有同日任务 | 新任务不得落同日 |
| 同一用户已有同时间段任务 | 新任务不得落同时间段 |
| 发布时间到达但生成失败 | 原窗口内重试 |
| 原窗口错过 | 顺延下一周 |
| 首页接口失败 | 展示错误态和重试 |
| 首页无内容 | 展示空态 |
| 搜索无结果 | 展示「没有找到『关键词』」 |
| 内容被设为不公开 | 首页公共流下架 |
| 内容被删除 | 首页、我的笔记、详情页同步不可见 |

## 11. 埋点需求

| 事件名 | 触发时机 | 关键属性 |
|---|---|---|
| `auto_publish_modal_show` | 首页授权弹窗展示 | `user_id`, `is_new_user`, `consent_version` |
| `auto_publish_allow_click` | 点击允许 | `source`, `consent_version` |
| `auto_publish_deny_click` | 点击暂不允许/关闭/蒙层 | `deny_type`, `consent_version` |
| `auto_publish_toggle_change` | 设置页开关变化 | `from_status`, `to_status` |
| `auto_publish_task_created` | 任务生成 | `week_start`, `execution_date`, `slot_code` |
| `auto_publish_task_execute` | 任务开始执行 | `task_id`, `scheduled_at` |
| `auto_publish_task_success` | 发布成功 | `task_id`, `post_id`, `retry_count` |
| `auto_publish_task_failed` | 发布失败 | `task_id`, `error_code`, `retry_count` |
| `auto_publish_task_deferred` | 顺延下一周 | `task_id`, `slot_code`, `fail_reason` |
| `whitebox_post_click` | 首页点击白盒卡片 | `post_id`, `source`, `category` |
| `whitebox_post_like` | 点赞 | `post_id`, `source` |
| `whitebox_post_comment` | 评论 | `post_id`, `source` |
| `whitebox_post_save` | 收藏 | `post_id`, `source` |

## 12. 权限与合规

1. 授权文案必须明确说明「依据名片标签或后台知识库自动生成笔记，并发布到平台展示」。
2. 用户必须可拒绝，拒绝后不能自动发布。
3. 用户必须可在设置页随时关闭。
4. 关闭后不得继续生成新的自动发布任务。
5. 用户删除账号或撤回授权时，未执行任务必须取消。
6. 自动发布内容需标识为 AI 自动生成。
7. 自动发布内容需支持删除和设为不公开。
8. 需记录授权协议版本、授权时间、授权来源。

## 13. 验收标准

### 13.1 授权弹窗

1. 老用户升级后首次进入首页，必须展示授权弹窗。
2. 新用户登录后进入 Tracy 聊天，完成或跳过进入首页，必须展示授权弹窗。
3. 点击允许后，弹窗消失，授权状态为 `enabled`。
4. 点击暂不允许、关闭、蒙层后，弹窗消失，授权状态为 `denied`。
5. 同一协议版本不重复弹出。

### 13.2 设置开关

1. 授权允许后，设置页「AI 智库自动发文」为开启。
2. 用户关闭开关后，未执行任务全部取消。
3. 用户重新开启后，按当前周剩余合法窗口补生成任务。

### 13.3 调度规则

1. 同一用户同一自然周最多发布 3 篇。
2. 3 篇必须分配到 3 个不同自然日。
3. 3 篇必须分配到 3 个不同时间段。
4. 不得在 S1/S2/S3/S4 之外发布。
5. 每周一 00:00:00 重置周配额。
6. 每周一预生成本周任务。
7. 失败只在原窗口内重试。
8. 窗口错过后顺延下一周。

### 13.4 首页展示

1. 自动发布成功后，首页白盒列表可见。
2. 自动发布成功后，我的主页笔记列表同步可见。
3. 自动发布内容展示 AI 标识。
4. 搜索可命中标题、正文、作者、分类、标签。
5. 分类 Tab 由后端动态返回并可正确筛选。
6. 点赞、评论、收藏、分享、关注、开聊交互可用。
7. 首页白盒列表为双列瀑布流，卡片高度随标题、作者信息和互动信息自动撑开；摘要不展示。
8. 长标题卡片不得被固定高度裁切，也不得与相邻卡片重叠。
9. 白盒打开后的颜色底图需读取后台配置，不得在客户端写死。
10. 图片底图加载失败时需回退到渐变色或全局默认配置。

### 13.5 运营后台视觉配置

1. 运营可创建全局、分类、标签、单篇内容四类配置。
2. 运营可选择纯色、渐变色、颜色卡图、展开底图。
3. 配置发布后，首页白盒列表和笔记详情页均能生效。
4. 单篇配置优先级高于标签，标签高于分类，分类高于全局默认。
5. 支持浅色/深色模式分别配置图片资源。
6. 支持预览、发布、下线、回滚。
7. 无有效配置时，前台使用全局默认渐变。
8. 支持普通、稀有两种底图等级配置，命中比例为 `90% / 10%`。
9. 支持稀有底图库内单图权重和主色配置，用于服务端命中和相邻 6 个白盒去重。
10. 客户端不得绕过后台配置自行内置稀有底图资源。

### 13.6 互动与名片统计

1. 笔记点赞后点赞数 +1，再次点击取消点赞后 -1。
2. 笔记收藏后收藏数 +1，再次点击取消收藏后 -1。
3. 评论提交成功后评论数 +1，评论列表即时展示新评论。
4. 回复评论后展示在对应父评论下。
5. 分享给好友、保存图片、复制链接均需记录分享埋点。
6. 名片页展示关注、粉丝、获赞与收藏、互动 4 项数据。
7. 关注成功后按钮变为已关注，对方粉丝数 +1。
8. 取消关注后按钮恢复关注，对方粉丝数 -1。
9. 获赞与收藏只统计公开且未删除内容。
10. 互动数按当前 `stats_version` 口径展示，不得无版本变更直接改口径。
11. 点击关注数进入关注列表，点击列表用户进入名片详情页。
12. 点击粉丝数进入粉丝列表，点击粉丝头像或整行进入粉丝名片详情页。
13. 点击获赞与收藏进入统计明细页，展示分项统计。
14. 点击互动无跳转，互动只展示数字。
15. 评论列表支持分页、排序、回复展开和审核中状态。
16. 分享成功、失败、取消均有明确反馈或埋点。

## 14. 测试用例重点

1. 周一 00:00 对 100 个授权用户批量生成任务。
2. 单用户一周生成 3 条任务，日期不重复、时间段不重复。
3. 单用户已使用 S1 后，补生成不得再使用 S1。
4. 单用户周三授权，只能生成周三之后的合法任务。
5. 周日 22:55 授权，当前周可用窗口不足时，不强行补发。
6. 任务在 S2 失败，09:55 仍可重试，10:00 后不得重试。
7. 用户在任务执行前关闭授权，任务取消。
8. 用户关闭授权后历史笔记仍保留。
9. 自动发布笔记设为不公开后，首页公共流不可见。
10. 首页搜索无结果展示空态。
11. 首页接口 500 展示错误态并支持重试。
12. 离线/弱网时列表保留缓存并提示网络异常。
13. 首页双列瀑布流在不同标题长度下高度自适应。
14. 分页加载后瀑布流重新排布，无重叠、无异常空洞。
15. 首页 Tab 由已发布内容动态生成，不存在无内容 Tab。
16. Tab 排序和置顶规则在接口返回中可验证。
17. 标签别名归一后不重复展示。
18. Tab 空状态回退到 `全部`。
19. 运营发布分类视觉配置后，对应分类白盒打开后展示新底图。
20. 单篇视觉配置覆盖分类配置。
21. 暗黑模式下优先展示暗黑底图；未配置时使用暗黑兜底渐变。
22. 视觉配置回滚后，前台展示恢复到上一版本。
23. 点赞、收藏失败时前端计数需回滚。
24. 评论为空不可提交。
25. 分享动作不改变点赞、收藏、评论计数。
26. 名片关注/取消关注后粉丝数和关注状态同步更新。
27. 不公开或删除笔记后，获赞与收藏统计需扣减。
28. 点击开聊并成功进入聊天页后，互动数按统计口径更新。
29. 点击关注数进入关注列表，点击列表用户进入名片详情页。
30. 点击粉丝数进入粉丝列表，点击粉丝头像进入粉丝名片详情页。
31. 点击获赞与收藏进入统计明细页，分项数据正确。
32. 点击互动统计项不跳转。
33. 评论分页、回复展开、删除评论、审核中评论状态正确。
34. 分享给好友、保存图片、复制链接分别记录成功/失败埋点。
35. 普通、稀有两等级底图均可命中并正常渲染，服务端整体命中比例符合 `90% / 10%`。
36. 同一用户重复打开同一白盒，底图和 `visual_config_id` 保持一致。
37. 用户连续打开或阅读 6 个白盒时，主色不得完全重复。
38. 快速退出、刷新、重复打开同一白盒不得提高稀有底图概率。
39. 点击白盒后先展示展开背景图或色块 `500-800ms`，随后进入笔记详情页。
40. 授权用户无名片标签且无后台知识库时，不生成自动发布任务。
41. 用户补充名片标签或后台知识库后，可进入自动发布候选池。
42. 自动生成笔记每次只调取 1 条名片标签或 1 条知识库内容，且同用户历史自动笔记内容不得重复。

## 15. 运营后台视觉配置 PRD

### 15.1 功能目标

为白盒打开后的展开底图、已读态颜色卡图、分类颜色和默认视觉风格提供后台配置能力。运营可不发版调整白盒视觉表现，降低客户端写死颜色、后续返工的风险。

### 15.2 使用角色

| 角色 | 权限 |
|---|---|
| 运营编辑 | 新建、编辑、预览草稿 |
| 运营负责人 | 发布、下线、回滚配置 |
| 管理员 | 全部权限、权限分配、强制下线 |
| 审核人员 | 查看资源、审核图片合规性 |

### 15.3 配置列表页

列表字段：

| 字段 | 说明 |
|---|---|
| 配置名称 | 运营自定义名称 |
| 生效范围 | 全局/分类/标签/单篇内容 |
| 生效对象 | 如 `考研`、`实习`、`post_id` |
| 视觉类型 | 纯色/渐变色/颜色卡图/展开底图 |
| 底图等级 | 普通/稀有 |
| 命中权重 | 同等级内的服务端权重 |
| 主色 | 用于相邻 6 个白盒去重 |
| 状态 | 草稿/已发布/已下线 |
| 最后更新人 | 操作人 |
| 最后更新时间 | 时间 |
| 操作 | 编辑、预览、发布、下线、回滚 |

筛选条件：

1. 状态。
2. 生效范围。
3. 视觉类型。
4. 配置名称关键词。
5. 更新时间。

### 15.4 新建/编辑配置页

必填字段：

| 字段 | 规则 |
|---|---|
| 配置名称 | 1-30 个字符 |
| 生效范围 | `global` / `category` / `tag` / `post` |
| 生效对象 | 全局为空；分类/标签/单篇必填 |
| 视觉类型 | 纯色/渐变色/颜色卡图/展开底图 |
| 底图等级 | `normal` / `rare` |
| 命中权重 | 1-1000，默认 100；仅稀有底图库内按权重二次抽取 |
| 主色 | HEX，如 `#9F97EB`，用于相邻 6 个白盒去重 |
| 浅色模式配置 | 必填 |
| 暗黑模式配置 | 可选；不填则使用暗黑兜底渐变 |
| 遮罩透明度 | 0-1，默认 0.18 |
| 文字色调 | `light` / `dark` / `auto`，默认 `auto` |

颜色输入规则：

1. 纯色必须输入 HEX，如 `#9F97EB`。
2. 渐变色至少 2 个色值，最多 4 个色值。
3. 渐变角度范围 0-360 度，默认 155 度。
4. 后台需提供颜色选择器和 HEX 手动输入。

图片上传规则：

| 类型 | 比例 | 最小尺寸 | 格式 | 大小 |
|---|---|---:|---|---:|
| 颜色卡图 | 3:4 | 900×1200px | PNG/JPG/WebP | ≤2MB |
| 展开底图 | 9:16 | 1080×1920px | PNG/JPG/WebP | ≤3MB |
| 暗黑模式图 | 与对应浅色图一致 | 与对应浅色图一致 | PNG/JPG/WebP | ≤3MB |

图片处理：

1. 上传后自动生成 WebP 压缩版本。
2. 保留原图，前台使用压缩图。
3. 服务端需生成缩略图用于后台列表。
4. 图片必须走内容安全审核。
5. 审核不通过不得发布。

### 15.5 预览

预览必须支持：

1. 首页白盒卡片预览。
2. 白盒打开后的详情页预览。
3. 浅色模式预览。
4. 深色模式预览。
5. 长标题预览。
6. 短标题预览。
7. 普通、稀有两种等级预览。
8. 相邻 6 个白盒主色去重预览。

预览数据：

1. 可选择系统默认示例白盒。
2. 可输入指定 `post_id` 预览真实内容。
3. 未发布草稿也可预览，但不影响线上。

### 15.6 发布、下线、回滚

发布规则：

1. 发布前校验配置完整性。
2. 发布前校验同一范围是否已有已发布配置。
3. 若同一范围已有已发布配置，新配置发布后旧配置自动进入历史版本。
4. 发布成功后生成配置版本记录。

下线规则：

1. 下线后该范围回退到下一优先级配置。
2. 全局默认配置不允许全部下线，必须至少保留 1 条已发布全局默认配置。

回滚规则：

1. 支持回滚到上一已发布版本。
2. 回滚也需要生成新版本记录。
3. 回滚后前台需在 10 分钟缓存期内自动更新，或由服务端版本号触发刷新。

### 15.7 前台生效逻辑

服务端在返回首页列表和详情接口时完成配置匹配：

1. 先查单篇 `post_id` 配置。
2. 未命中则查标签配置。
3. 未命中则查分类配置。
4. 未命中则查全局默认配置。

客户端只渲染接口返回的 `visual_config`，不得自行推导分类色。

### 15.8 后台异常

| 场景 | 处理 |
|---|---|
| 图片上传失败 | 提示重新上传 |
| 图片尺寸不合规 | 禁止保存并提示规格 |
| 图片审核失败 | 禁止发布 |
| 同范围配置冲突 | 发布时提示将覆盖旧配置 |
| 全局默认缺失 | 禁止下线最后一条全局配置 |
| 前台配置读取失败 | 使用客户端内置兜底渐变 |

## 16. 开发拆分建议

后端：

1. 授权状态接口。
2. 首页白盒列表与详情接口。
3. 自动发布任务表、周配额表。
4. 每周一任务预生成 Job。
5. 周中授权补生成逻辑。
6. 任务执行、失败重试、顺延下一周。
7. 自动发布内容入库与首页流同步。
8. 白盒视觉配置表、版本表。
9. 运营后台视觉配置接口。
10. 首页/详情接口返回命中的视觉配置。
11. 白盒 Tab 配置表和标签别名映射表。
12. 首页 Tab 列表接口，按已发布内容动态返回 Tab。
13. 笔记点赞、收藏、分享、评论、回复接口。
14. 关注关系接口和名片统计接口。
15. 名片统计聚合任务与实时增量更新。
16. 关注列表、粉丝列表、获赞与收藏明细接口。
17. 开聊计数接口和防重复计数处理。
18. 白盒底图 `90%普通 / 10%稀有` 命中算法和用户底图绑定记录。
19. 相邻 6 个白盒主色去重、防刷新刷概率、防重复打开刷概率。

算法：

1. 依据单条名片标签或单条后台知识库内容生成标题、正文、分类、标签。
2. 内容去重和质量评分。
3. 内容安全审核前置检查。

客户端：

1. 首页授权弹窗。
2. 设置页自动发文开关。
3. 首页白盒列表接接口。
4. 白盒详情页接接口。
5. 我的主页笔记同步展示。
6. 弱网、空态、错误态。
7. 双列瀑布流布局自适应。
8. 根据服务端 `visual_config` 渲染已读态和展开底图。
9. 暗黑模式视觉配置兜底。
10. 根据服务端返回的 Tab 列表渲染分类，不在前端写死枚举。
11. 当前 Tab 失效、为空或接口异常时自动回退到 `全部`。
12. 点赞、收藏、评论、关注的乐观更新和失败回滚。
13. 名片页关注、粉丝、获赞与收藏、互动统计展示。
14. 分享面板与分享行为上报。
15. 关注列表、粉丝列表、获赞与收藏明细页面。
16. 关注/粉丝列表点击进入名片详情页。
17. 互动统计项不可点击。
18. 渲染服务端返回的 `rarity_level`、`main_color`、`visual_seed` 和底图资源，不在客户端计算稀有概率。
19. 白盒详情页上报有效阅读、点赞、收藏、评论、分享行为，用于内容质量、推荐排序和数据分析，不用于提高稀有底图概率。

运营后台：

1. 视觉配置列表页。
2. 新建/编辑配置页。
3. 图片上传和审核状态展示。
4. 浅色/深色/长短标题预览。
5. 发布、下线、回滚能力。
6. Tab 置顶、排序、别名归一配置能力。
7. 底图等级、命中权重、主色配置能力。
8. 普通/稀有两等级资源管理和预览，稀有底图支持单图权重配置。

测试：

1. 授权流程回归。
2. 调度算法单元测试。
3. 定时任务集成测试。
4. 首页展示与互动回归。
5. 权限关闭、删除、不公开等异常链路。
6. 瀑布流高度自适应回归。
7. 运营后台视觉配置生效、覆盖、回滚测试。
8. 动态 Tab 生成、排序、归一和失效回退测试。
9. 点赞、收藏、评论、分享、关注状态与计数回归。
10. 名片统计口径和聚合延迟回归。
11. 关注/粉丝列表跳转名片详情回归。
12. 获赞与收藏明细统计口径回归。
13. 互动不可点击和开聊计数回归。
14. 评论分页、回复展开、审核中状态回归。
15. 普通/稀有底图命中率、稀有单图权重和回退测试。
16. 同一用户同一白盒重复打开底图一致性测试。
17. 相邻 6 个白盒主色不重复测试。
18. 快速退出、刷新、重复打开不增加稀有概率测试。
