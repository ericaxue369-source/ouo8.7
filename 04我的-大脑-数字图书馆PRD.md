# 我的-大脑-数字图书馆 PRD V1.0

更新时间：2026-08-11  
对应原型：`https://ericaxue369-source.github.io/ouo8.7/`  
关联文档：`UI改版技术交付文档 V1.0.md`、`本阶段开发交付清单.md`  
面向对象：产品、iOS、Android、鸿蒙、后端、算法、测试

## 1. 开发必读

1. 本 PRD 只覆盖「我的 → 大脑 → 数字图书馆」模块。
2. 本 PRD 不包含「我的 → 大脑 → 知识」模块；知识模块已完成开发，本阶段只在交付清单中补遗漏项。
3. 数字图书馆内容不影响首页白盒自动发布。
4. 白盒自动发布内容来源为用户名片标签或后台存储知识库内容，由 AI 生成笔记；不读取数字图书馆周报内容作为生成依据。
5. 数字图书馆在前台表现为每周自动生成的成长周报列表，用户可进入「和这周的自己聊聊」。
6. 视觉、颜色、字体、间距、圆角、暗黑模式、安全区、动态字体按 `UI改版技术交付文档 V1.0.md` 执行。
7. 手机端锁定竖屏；折叠屏和平板按 UI 交付文档的宽度断点适配。

## 2. 背景与目标

数字图书馆用于沉淀用户每周的成长轨迹。系统根据用户在平台内的学习、聊天、训练、资料更新等行为，自动形成一条周报记录。用户可以回看每周摘要，也可以点击「和这周的自己聊聊」进入自我对话页面，与该周数据形成的 AI 上下文聊天。

目标：

1. 在「我的 → 大脑」下提供「数字图书馆」Tab。
2. 展示按周生成的数字周报列表。
3. 标识当前自然周周报。
4. 支持点击周报进入「和这周的自己聊聊」聊天页。
5. 支持默认、加载、空、错误、无网络、部分加载失败、暗黑模式和大字体状态。
6. 与「知识」模块和「白盒自动发布」模块保持数据边界清晰。

## 3. 需求范围

包含：

1. 数字图书馆 Tab 展示。
2. 周报列表展示。
3. 当前周标识。
4. 周报摘要展示。
5. 周报自我对话入口。
6. 周报自我对话页。
7. 周报数据接口、分页、状态、埋点和验收。

不包含：

1. 知识创建、知识训练、附件训练、知识编辑、知识删除。
2. 白盒自动发布任务生成与调度。
3. 白盒笔记生成内容策略。
4. 周报后台运营编辑系统。
5. 对外分享周报能力。V1.1 再评估。

## 4. 信息架构

```text
我的
  └─ 大脑
      ├─ 数字图书馆
      │   ├─ 周报列表
      │   └─ 和这周的自己聊聊
      └─ 知识
          └─ 不属于本 PRD 范围
```

## 5. 页面需求

### 5.1 数字图书馆列表页

入口：我的 → 大脑 → 数字图书馆。  
页面 ID：`P08_BRAIN_DIGITAL_LIBRARY`。

页面结构：

| 区域 | 规则 |
|---|---|
| 顶部 Tab | 与原型一致，展示 `数字图书馆`、`知识` 两个 Tab；本 PRD 只定义数字图书馆 Tab |
| 说明文案 | `每周自动生成，与你的分身对话，回顾这一周的成长轨迹。` |
| 周报卡片列表 | 按周倒序排列，最近一周在最上方 |
| 当前周标识 | 当前自然周周报展示 `本周` 胶囊 |
| CTA | 每张卡片展示 `和这周的自己聊聊 →` |

卡片字段：

| 字段 | 类型 | 规则 |
|---|---|---|
| `report_id` | string | 周报唯一 ID |
| `week_label` | string | 展示周次，如 `2026.3 第三周` |
| `week_start_date` | date | 自然周开始日期 |
| `week_end_date` | date | 自然周结束日期 |
| `summary` | string | 周报摘要，列表最多展示 3 行 |
| `is_current_week` | boolean | true 时展示 `本周` |
| `unread_count` | integer | 当前周报未读数；未阅读时默认 `1`，已阅读为 `0` |
| `is_read` | boolean | 当前用户是否已阅读该周报 |
| `generated_at` | datetime | 周报生成时间 |
| `status` | enum | `generating` / `ready` / `failed` |
| `chat_session_id` | string | 已有自我对话会话 ID，可为空 |

视觉规则：

| 元素 | 规格 |
|---|---|
| 页面边距 | iOS 16pt；Android 16dp；鸿蒙 24vp |
| 卡片圆角 | 16 |
| 卡片内距 | 16 |
| 卡片间距 | 10 |
| 周报标题 | 14 Semibold，行高 20 |
| 摘要 | 13 Regular，行高 21，最多 3 行 |
| 本周胶囊 | 字号 12，圆角 999，左右内距 8，上下内距 2 |
| 未读计数 | 紫色圆形/胶囊角标，白色数字；`unread_count=0` 时隐藏 |
| CTA 按钮 | 视觉高 32，圆角 999，点击热区 iOS 44 / Android 48 / 鸿蒙 48 |

交互规则：

1. 点击周报卡片或 CTA，进入 `P08_WEEKLY_SELF_CHAT`。
2. 若 `status=generating`，卡片展示生成中状态，CTA 禁用，文案为 `本周周报生成中`。
3. 若 `status=failed`，卡片展示失败提示，CTA 改为 `重新生成`。
4. 点击 `重新生成` 调用重新生成接口；请求中按钮 loading 且禁用。
5. 列表支持分页；默认每页 20 条。
6. 下拉刷新更新列表和当前周状态。
7. 上拉加载距底部 50pt/dp/vp 自动触发。
8. 若周报 `unread_count>0`，卡片右上角展示未读计数；当前版本单条周报默认最多展示 `1`。
9. 数字图书馆 Tab 文案右上角展示所有未读周报 `unread_count` 总和。
10. 用户点击周报卡片或 CTA 进入自我对话页后，该周报标记为已读，卡片角标消失，Tab 汇总计数同步减少。

### 5.2 周报自我对话页

页面 ID：`P08_WEEKLY_SELF_CHAT`。  
入口：数字图书馆周报卡片 CTA。

页面结构：

| 区域 | 规则 |
|---|---|
| 顶部导航 | 返回按钮 + 标题 `和这周的自己聊聊` |
| 周报上下文卡 | 展示周次和摘要，可折叠 |
| 消息列表 | 展示用户与该周 AI 上下文的对话 |
| 底部输入区 | 胶囊输入框 + 发送按钮，避让键盘和底部安全区 |

消息字段：

| 字段 | 类型 | 规则 |
|---|---|---|
| `message_id` | string | 消息唯一 ID |
| `report_id` | string | 所属周报 ID |
| `role` | enum | `user` / `assistant` / `system` |
| `content` | string | 消息内容 |
| `status` | enum | `sending` / `sent` / `failed` / `generating` |
| `created_at` | datetime | 创建时间 |

交互规则：

1. 首次进入无历史消息时，消息列表第一条必须展示当周周报内容。
2. 第一条消息角色为 `assistant` 或 `system` 均可，但前台按左侧 AI 气泡展示；内容使用该周报 `summary` 或完整周报正文。
3. 周报内容第一条消息永远置于该会话最前，不允许被用户删除、撤回或排序到后面。
4. 第一条消息之后，AI 可继续追加引导语，例如：`这是你这一周的数字轨迹。想先从哪一部分聊起？`
5. 用户输入为空时，发送按钮禁用。
6. 单条用户输入最多 1000 字。
7. 发送后本地先插入用户消息，状态为 `sending`。
8. 发送成功后状态改为 `sent`，AI 回复进入 `generating`。
9. AI 回复成功后插入 assistant 消息。
10. 发送失败时，消息气泡展示重试入口。
11. 退出页面后保留历史消息；再次进入同一周报恢复会话。
12. 该聊天只读取当前周报上下文，不写入白盒自动发布内容池。
13. 后续对话必须基于当前周报建立上下文记忆，只回答与该周周报、用户当周知识库轨迹和用户提问相关的内容；不得跨周自动混入其他周报上下文。

## 6. 页面状态

| 页面 | 状态 | 触发条件 | 表现 | 操作 |
|---|---|---|---|---|
| 数字图书馆列表 | 默认态 | 接口返回周报列表 | 展示周报卡片 | 点击进入自我对话 |
| 数字图书馆列表 | 首次加载 | 首次进入 | 3 条周报卡片骨架 | 无 |
| 数字图书馆列表 | 下拉刷新 | 用户下拉 | 顶部刷新指示器 | 请求结束回弹 |
| 数字图书馆列表 | 上拉加载 | 距底 50 | 底部 loading | 失败显示点击重试 |
| 数字图书馆列表 | 空态 | 无任何周报 | 文案 `还没有数字周报`，说明 `完成更多对话和训练后，每周会自动生成成长轨迹` | 可返回我的页 |
| 数字图书馆列表 | 错误态 | 接口 500/解析失败 | 错误提示 + 重试按钮 | 点击重试 |
| 数字图书馆列表 | 无网络 | 网络不可用 | 顶部 Banner，保留缓存 | 网络恢复自动刷新 |
| 周报卡片 | 生成中 | 当前周报生成中 | 标题 + `生成中` 状态，CTA 禁用 | 无 |
| 周报卡片 | 生成失败 | 周报生成失败 | 错误提示，CTA `重新生成` | 点击重新生成 |
| 自我对话页 | 默认态 | 有消息历史 | 消息列表 + 输入区 | 发送消息 |
| 自我对话页 | 空态 | 无历史消息 | AI 开场白 | 输入回复 |
| 自我对话页 | 发送中 | 用户点击发送 | 用户气泡 loading | 禁止重复发送同一条 |
| 自我对话页 | AI 生成中 | 等待 AI 回复 | assistant typing 气泡 | 可继续浏览历史 |
| 自我对话页 | 发送失败 | 消息接口失败 | 气泡内 `重试` | 点击重试 |
| 自我对话页 | 无网络 | 网络不可用 | 顶部 Banner，发送按钮禁用 | 网络恢复后恢复发送 |

## 7. 数据边界

### 7.1 与知识模块的关系

1. 数字图书馆和知识模块同在「我的 → 大脑」下，但业务数据独立。
2. 数字图书馆展示系统自动生成的周报记录。
3. 知识模块管理用户可训练、可组合上架咨询的知识资产。
4. 本阶段不改知识模块业务，只补遗漏展示项：训练时长统计、附件统计。

### 7.2 与白盒自动发布的关系

1. 数字图书馆内容不会影响白盒自动发布。
2. 白盒自动发布不读取数字图书馆周报摘要。
3. 白盒自动发布依据用户名片标签或后台存储知识库内容，由 AI 生成笔记。
4. 若用户在数字图书馆聊天中产生新信息，该信息是否进入后台知识库不属于本阶段范围；默认不进入。
5. 后续如要把数字图书馆聊天沉淀为知识库，需要另立 PRD 和用户授权。

## 8. 接口需求

### 8.1 查询数字图书馆周报列表

`GET /api/v1/brain/digital-library/reports`

参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `cursor` | string | 否 | 分页游标 |
| `pageSize` | int | 否 | 默认 20，最大 50 |

返回：

```json
{
  "items": [
    {
      "reportId": "wr_202603_w3",
      "weekLabel": "2026.3 第三周",
      "weekStartDate": "2026-03-16",
      "weekEndDate": "2026-03-22",
      "summary": "本周与陈思远完成2次保研模拟面试，上传个人陈述草稿至知识库，AI训练时长6小时。",
      "isCurrentWeek": true,
      "unreadCount": 1,
      "isRead": false,
      "generatedAt": "2026-03-22T23:30:00+08:00",
      "status": "ready",
      "chatSessionId": "chat_1001"
    }
  ],
  "totalUnreadCount": 3,
  "nextCursor": "cursor_next",
  "hasMore": true
}
```

返回规则：

1. `totalUnreadCount` 为当前用户全部未读周报总数，不只统计当前分页。
2. 前端数字图书馆 Tab 角标使用 `totalUnreadCount`，不得只用当前页累加值。

### 8.2 标记周报已读

`POST /api/v1/brain/digital-library/reports/{reportId}/read`

触发时机：用户点击周报卡片或 CTA，成功进入 `P08_WEEKLY_SELF_CHAT` 时调用。

返回：

```json
{
  "reportId": "wr_202603_w3",
  "isRead": true,
  "unreadCount": 0,
  "totalUnreadCount": 2
}
```

规则：

1. 接口需幂等，重复调用不重复扣减。
2. 标记已读后，该周报卡片右上角未读计数隐藏。
3. 数字图书馆 Tab 汇总未读计数同步更新为 `totalUnreadCount`。
4. 若进入自我对话页失败，不标记已读。

### 8.3 重新生成周报

`POST /api/v1/brain/digital-library/reports/{reportId}/regenerate`

返回：

```json
{
  "reportId": "wr_202603_w3",
  "status": "generating",
  "message": "周报重新生成中"
}
```

规则：

1. 只允许对 `failed` 或运营允许重试的周报重新生成。
2. 同一周报 10 分钟内最多触发 1 次重新生成。
3. 重新生成中前端按钮禁用。

### 8.4 查询周报自我对话消息

`GET /api/v1/brain/digital-library/reports/{reportId}/chat/messages`

参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `cursor` | string | 否 | 历史消息分页游标 |
| `pageSize` | int | 否 | 默认 30 |

返回字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `messageId` | string | 消息 ID |
| `role` | enum | `user` / `assistant` / `system` |
| `content` | string | 消息内容 |
| `status` | enum | `sent` / `failed` |
| `createdAt` | datetime | 创建时间 |

### 8.5 发送周报自我对话消息

`POST /api/v1/brain/digital-library/reports/{reportId}/chat/messages`

请求：

```json
{
  "content": "这周我在哪些地方进步最大？",
  "clientMessageId": "local_172290001"
}
```

返回：

```json
{
  "userMessage": {
    "messageId": "msg_1001",
    "role": "user",
    "content": "这周我在哪些地方进步最大？",
    "status": "sent",
    "createdAt": "2026-08-06T15:10:00+08:00"
  },
  "assistantMessage": {
    "messageId": "msg_1002",
    "role": "assistant",
    "content": "你这周最大的进步是把目标从模糊想法拆成了可执行计划。",
    "status": "sent",
    "createdAt": "2026-08-06T15:10:05+08:00"
  }
}
```

## 9. 数据结构建议

### 9.1 数字周报表

表名：`brain_weekly_reports`

| 字段 | 类型 | 说明 |
|---|---|---|
| `report_id` | bigint | 主键 |
| `user_id` | bigint | 用户 ID |
| `week_start_date` | date | 周开始日期 |
| `week_end_date` | date | 周结束日期 |
| `week_label` | string | 展示周次 |
| `summary` | string | 周报摘要 |
| `full_content` | text | 周报完整内容，用于自我对话第一条消息 |
| `source_snapshot_id` | bigint | 生成周报使用的数据快照 ID |
| `is_read` | boolean | 当前用户是否已读，默认 false |
| `unread_count` | int | 未读数，默认 1，已读后为 0 |
| `read_at` | datetime | 首次阅读时间 |
| `status` | enum | `generating` / `ready` / `failed` |
| `fail_reason` | string | 失败原因 |
| `generated_at` | datetime | 生成时间 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

唯一索引：`user_id + week_start_date`

### 9.2 周报自我对话表

表名：`brain_weekly_report_messages`

| 字段 | 类型 | 说明 |
|---|---|---|
| `message_id` | bigint | 主键 |
| `report_id` | bigint | 周报 ID |
| `user_id` | bigint | 用户 ID |
| `role` | enum | `user` / `assistant` / `system` |
| `content` | text | 消息内容 |
| `status` | enum | `sent` / `failed` / `hidden` |
| `client_message_id` | string | 客户端本地消息 ID，防重复 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

唯一索引：`report_id + client_message_id`

## 10. 生成规则

1. 数字周报按自然周生成，自然周按北京时间 `Asia/Shanghai` 计算。
2. 每周日 `23:00:00` 触发生成当周周报；若任务积压，可进入排队，但 `generated_at` 需记录实际生成完成时间。
3. 每个用户每周最多生成 1 条数字周报。
4. 周报来源为用户当周后台知识库内容和知识库相关训练/沉淀记录。
5. 若用户当周知识库数据不足，可生成轻量周报，摘要说明 `这一周记录还不多，可以多和分身聊聊。`
6. 周报生成完成后建立一条列表周报条目，状态为 `ready`，`unread_count=1`，`is_read=false`。
7. 当前周周报可处于 `generating` 状态，生成完成后变为 `ready`。
8. 周报摘要建议 40-120 个中文字符；完整周报内容用于进入自我对话页后的第一条消息。
9. 周报不得自动发布到首页白盒内容流。
10. 周报不得作为白盒自动发布的直接内容来源。

自我对话上下文规则：

1. 用户点击周报进入「和这周的自己聊聊」时，如无历史会话，服务端需先创建会话，并把当周周报内容写入第一条消息。
2. 第一条消息必须固定为当周周报内容，是后续对话的核心上下文。
3. 后续 AI 回复必须基于该周报上下文和当前会话历史生成，不得默认读取其他自然周周报。
4. 同一周报再次进入时恢复原会话，不重复插入第一条周报消息。

## 11. 异常与边界

| 场景 | 处理 |
|---|---|
| 用户无周报 | 展示空态 |
| 当前周生成中 | 展示生成中卡片，CTA 禁用 |
| 生成失败 | 展示失败卡片，支持重新生成 |
| 重新生成频繁 | Toast：操作太频繁，请稍后再试 |
| 周报摘要超长 | 列表最多 3 行省略 |
| 周报摘要为空 | 展示 `本周暂无足够内容生成摘要` |
| 消息超长 | 单条最多 1000 字，超出禁止发送 |
| 发送失败 | 气泡内重试 |
| 历史消息加载失败 | 顶部局部错误，点击重试 |
| 无网络 | 保留缓存，发送禁用 |
| 后台恢复 | 离开后台超过 5 分钟，返回后刷新列表；自我对话页不覆盖未发送输入 |

## 12. 埋点需求

| 事件名 | 触发时机 | 关键属性 |
|---|---|---|
| `digital_library_tab_view` | 进入数字图书馆 Tab | `user_id` |
| `weekly_report_card_view` | 周报卡片曝光 | `report_id`, `week_start_date`, `status` |
| `weekly_report_click` | 点击周报卡片或 CTA | `report_id`, `status` |
| `weekly_report_read` | 周报标记已读 | `report_id`, `before_unread_count`, `after_total_unread_count` |
| `weekly_report_regenerate_click` | 点击重新生成 | `report_id`, `fail_reason` |
| `weekly_self_chat_open` | 进入自我对话页 | `report_id`, `chat_session_id` |
| `weekly_self_chat_send` | 发送消息 | `report_id`, `message_length` |
| `weekly_self_chat_reply_success` | AI 回复成功 | `report_id`, `latency_ms` |
| `weekly_self_chat_failed` | 发送或回复失败 | `report_id`, `error_code` |

## 13. 验收标准

1. 我的 → 大脑下展示数字图书馆 Tab。
2. 数字图书馆列表按周倒序展示。
3. 当前自然周周报展示 `本周` 胶囊。
4. 周报摘要最多 3 行，不撑破卡片。
5. 点击「和这周的自己聊聊」进入周报自我对话页。
6. 自我对话页顶部展示返回和标题。
7. 自我对话页底部输入框避让键盘和底部安全区。
8. 输入为空时发送按钮禁用。
9. 单条输入超过 1000 字禁止发送。
10. 发送失败可重试。
11. 周报生成中时 CTA 禁用。
12. 周报失败时支持重新生成。
13. 数字图书馆内容不进入白盒自动发布内容池。
14. 白盒自动发布不读取数字图书馆周报摘要。
15. 暗黑模式下文字、卡片、按钮对比清晰。
16. 系统字体放大到 200% 时，卡片高度自适应且按钮可点击。
17. 无网络、接口失败、空态、部分加载失败均有明确展示。
18. 每周日 23:00 后生成当周周报，每个用户每周最多 1 条。
19. 新生成周报默认未读，周报卡片右上角展示 `1`，数字图书馆 Tab 展示未读总数。
20. 点击周报卡片或 CTA 成功进入自我对话页后，该周报未读计数清零，Tab 汇总同步减少。
21. 自我对话页第一条消息永远展示当周周报内容。
22. 周报自我对话只基于当前周报上下文，不混入其他周报，也不影响白盒自动发布内容池。
